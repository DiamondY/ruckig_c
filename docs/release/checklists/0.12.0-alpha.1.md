# Ruckig C 0.12.0-alpha.1 Waypoint Soft-Interruption True Resume Checklist

This checklist records the local `0.12.0-alpha.1` evidence slice on the
`0.12.0-design - Unreleased` line. It is not a stable `v0.12.0` release
closeout.

## Scope

- [x] Current stable release remains `v0.11.0`.
- [x] Current `main` remains `0.12.0-design - Unreleased`.
- [x] The slice is limited to intermediate-waypoint `ruckig_update` soft
  interruption.
- [x] Public `ruckig_calculate` remains a complete waypoint calculation path
  even when `interrupt_calculation_duration` is set.
- [x] No-waypoint target solving and tracking remain outside soft-interruption
  resume scope.
- [x] No public C ABI, public header signature, enum numeric value, result-code
  numeric value, or exported symbol is added.
- [x] Runtime platform-clock public hooks remain deferred.
- [x] Package-manager recipes/publication, Cloud/remote runtime, Pro/cloud
  equivalence claims, hard real-time claims, and formal global optimality
  claims remain out of scope.
- [x] No version bump, tag, GitHub Release, or manual release-random workflow
  is part of this alpha evidence slice.
- [x] `original/ruckig-main` remains frozen and unmodified.

## Implemented Behavior

- [x] Interrupted waypoint `ruckig_update` calculations keep private optimizer
  resume state after publishing the first complete feasible candidate.
- [x] Private resume state stores the search phase and complete-candidate
  cursors for baseline, finite-difference, refine, and branch-search work.
- [x] Branch queue storage is preallocated with the `ruckig_t` handle and does
  not allocate during online update/resume.
- [x] Background online cycles continue optimizer work only after normal
  `ruckig_output_pass_to_input` progression and matching private planning
  identity.
- [x] Background publish happens only for a complete feasible candidate that is
  better than the old trajectory's remaining duration.
- [x] Publishing a background candidate follows normal new-calculation output
  semantics: `new_calculation=true`, output time/section reset, then the cycle
  samples at `delta_time`.
- [x] Background interruption without a publishable candidate keeps sampling
  the old trajectory and returns `RUCKIG_WORKING`.
- [x] Clearing the interrupt field, changing planning identity, providing a
  non-normal current state, or calling `ruckig_reset` invalidates private
  resume state.

## Local Verification

| Command | Result |
| --- | --- |
| `cmake --build --preset windows-clang-ninja` | Passed |
| `ctest --test-dir out\build\windows-clang-ninja --output-on-failure -R "ruckig_c_waypoint_optimizer\|ruckig_c_per_section_constraints\|ruckig_c_waypoint_quality\|ruckig_c_allocation_audit\|ruckig_c_tests"` | 5/5 passed |
| `ctest --test-dir out\build\windows-clang-ninja --output-on-failure` | 46/46 passed |
| `cmake --build --preset windows-clang-ninja-shared` | Passed |
| `ctest --test-dir out\build\windows-clang-ninja-shared --output-on-failure` | 46/46 passed |
| `cmake --build out\build\windows-clang-ninja-duration --config Debug` | Passed |
| `ctest --test-dir out\build\windows-clang-ninja-duration --output-on-failure` | 46/46 passed with `RUCKIG_C_ENABLE_CALCULATION_DURATION=ON` |
| `cmake --build out\build\windows-clang-ninja-shared --target ruckig_c_verify_public_symbols` | Passed |
| `cmake --build out\build\windows-clang-ninja-shared --target ruckig_c_compare_public_exported_symbols` | Passed; public exported symbols match the approved allowlist |
| Public/export symbol audit | Public symbol count `172`; unapproved exported symbol count `0` |
| `zig cc -target x86_64-linux-gnu -std=c99 -Wall -Wextra -Wpedantic -D_POSIX_C_SOURCE=200809L -Iinclude -Isrc -c src\ruckig_c\waypoint.c -o "$env:TEMP\ruckig_c_waypoint_linux_probe.o"` | Passed |
| `zig cc -target x86_64-linux-gnu -std=c99 -Wall -Wextra -Wpedantic -DRUCKIG_C_PLATFORM_CLOCK_HEADER=<platform_clock_custom_provider.h> -DRUCKIG_C_CUSTOM_MONOTONIC_TIME_US=ruckig_test_platform_clock_us -Iinclude -Isrc -Itest\c -c src\ruckig_c\waypoint.c -o "$env:TEMP\ruckig_c_waypoint_custom_clock_probe.o"` | Passed |
| `out\build\windows-clang-ninja-oracle\ruckig_c_oracle_tests.exe` | Fixed oracle passed: 82 comparisons; waypoint section oracle passed: 4 comparisons |
| `out\build\windows-clang-ninja-oracle\ruckig_c_oracle_tests.exe --random 10000 --seed 1` | Passed |
| `out\build\windows-clang-ninja-oracle\ruckig_c_oracle_tests.exe --random-per-dof 10000 --seed 1` | Passed |
| `out\build\windows-clang-ninja-performance\ruckig_c_performance_benchmark.exe --samples 10000 --seed 1` | Passed; average ratio `1.02735` under release threshold `1.5` |
| `out\build\windows-clang-ninja-performance\ruckig_c_performance_benchmark.exe --samples 10000 --seed 1 --waypoints` | Passed; waypoint alpha benchmark average `3.29881e+06 ns`, p99 `1.21819e+07 ns` |
| Boundary diffs for public header, ABI docs, workflow files, package-manager paths, and `original/ruckig-main` | Empty |
| `git diff --check` | Passed; only CRLF working-copy warnings |

## Alpha Evidence Completion

- [x] Full default Windows clang/Ninja CTest.
- [x] Shared build and ABI/export symbol checks.
- [x] Duration-enabled build and CTest.
- [x] Platform clock POSIX/custom compile probes.
- [x] Oracle fixed/random smoke and waypoint performance trend.
- [x] `git diff --check` and boundary diffs for public header, ABI docs,
  workflow files, package-manager files, and `original/ruckig-main`.
- [x] Ordinary remote push CI for `2f689b2` completed successfully.

## Remote CI Evidence

This records ordinary push CI evidence for the local alpha implementation
commit. It is not a stable `v0.12.0` release closeout.

| Field | Value |
| --- | --- |
| Commit | `2f689b2 Implement waypoint soft interruption true resume` |
| Head SHA | `2f689b2909b1dcf5b4814e9c1d5e9801bfa55709` |
| Remote | `ruckig_c/main` |
| Workflow | `CI` |
| Event | `push` |
| Run ID | `27275153996` |
| Run URL | `https://github.com/DiamondY/ruckig_c/actions/runs/27275153996` |
| Status | `completed` |
| Conclusion | `success` |

Boundary checks after the successful CI run:

- [x] No local `v0.12.0*` tag was present.
- [x] No remote `v0.12.0*` tag was present on `ruckig_c`.
- [x] No GitHub Release for `v0.12.0*` was present; the latest release remained
  `v0.11.0`.
- [x] No manual release-random workflow was run for this alpha evidence step.
  In the ordinary push CI run, `Manual release random oracle` was skipped
  because the event was `push`.

## Deferred Outside This Alpha

Full release-readiness audit, version bump, tag, GitHub Release, and manual
release-random remain separate future work if this alpha is promoted toward a
stable release.
