# Ruckig C 0.7.0 Release Checklist

This checklist records the stable `v0.7.0` Optimized tracking strategy and
diagnostics release closeout. The release stabilizes the 172-symbol public C
ABI reviewed during `0.7.0-readiness`: the stable `v0.6.0` bounded local
Optimized tracking API plus high-level strategy preset controls and the public
diagnostics snapshot getter. It does not claim formal global optimality,
source-level tracking oracle parity, or Pro/cloud numerical equivalence.

## Scope

- [x] Version is `0.7.0` in `CMakeLists.txt`.
- [x] Version macros in `include/ruckig_c/ruckig.h` are `0.7.0`.
- [x] `CHANGELOG.md` has a dated `0.7.0` release entry.
- [x] `README.md` describes `v0.7.0` as the current stable release.
- [x] `docs/current/roadmap.md` records `v0.7.0` as the stable strategy and
  diagnostics release.
- [x] Existing `v0.6.0` public functions are not removed or signature-changed.
- [x] Existing enum numeric values and result-code numeric values are
  unchanged.
- [x] The public C strategy preset controls are stabilized in `v0.7.0`.
- [x] The public C diagnostics snapshot getter is stabilized in `v0.7.0`.
- [x] Local Fast and bounded Optimized online/offline tracking behavior remain
  included.
- [x] Python `cffi` remains prototype-only; no wheel is published.
- [x] Rust remains an alpha wrapper; no crate is published.
- [x] Package-manager recipes remain deferred.
- [x] Algorithm visualization/gallery generation remains deferred until after
  `v0.7.0`.
- [x] No Pro/cloud sample, network dependency, or external license is used as a
  release gate.
- [x] `original/ruckig-main` remains frozen as Ruckig Community `0.17.3`.

## Public C ABI Review

- [x] `include/ruckig_c/ruckig.h` contains the approved 172-symbol public C ABI
  and no closeout-only public API expansion.
- [x] `docs/abi/public-symbols.txt` contains the 172-symbol public allowlist.
- [x] `docs/abi/public-symbol-exceptions.txt` records the approved
  `0.7.0-alpha` strategy and `0.7.0-alpha.2` diagnostics additions.
- [x] ABI artifact paths use `artifacts/abi/0.7.0`.
- [ ] Public header diff against `v0.6.0` is reviewed:

  ```powershell
  git -c safe.directory=E:/Yww/DownLoad/source/ruckig_c diff v0.6.0 -- include/ruckig_c/ruckig.h
  ```

- [ ] Public header diff contains only the approved strategy enum, strategy
  controls, diagnostics snapshot, version macros, and release scope comments.
- [ ] Public symbol allowlist verification passes.
- [ ] Public exported-symbol comparison passes.
- [ ] Current public symbols: `172`.
- [ ] Public removals: `0`.
- [ ] Unapproved exported symbols: `0`.
- [ ] Tracking internals are not exported.

## Tracking Release Evidence

- [x] `v0.7.0` stabilizes `Stable`, `Balanced`, and `Aggressive` Optimized
  strategy presets.
- [x] `Balanced` remains the default strategy.
- [x] The public diagnostics snapshot covers Fast, Optimized, online,
  lookahead, and offline sequence calls.
- [x] `Optimized` mode remains a bounded deterministic local evaluator with
  Fast fallback.
- [x] `Optimized` mode does not claim formal global optimality or Pro/cloud
  numerical equivalence.
- [x] `interrupt_calculation_duration` does not create tracking timeout,
  soft-interruption, or hard real-time guarantees.
- [ ] C tracking tests cover strategy presets, diagnostics snapshots, fallback
  diagnostics, quality gates, deterministic random stress, and no-allocation
  paths.
- [ ] Python prototype smoke covers strategy presets and diagnostics snapshots.
- [ ] Rust alpha smoke covers strategy presets, diagnostics snapshots, and
  examples.

## Required Local Gates

- [ ] Static Windows clang/Ninja build passes.
- [ ] Shared Windows clang/Ninja build passes.
- [ ] Oracle Windows clang/Ninja build passes.
- [ ] Performance Windows clang/Ninja build passes.
- [ ] Static CTest without release random passes.
- [ ] Shared CTest without release random passes.
- [ ] Tracking CTest gates pass.
- [ ] Solver branch coverage CTest gate passes.
- [ ] Fixed oracle suite passes.
- [ ] Ordinary random oracle seeds `1`, `2`, and `41` pass with `100000`
  samples each.
- [ ] Per-DoF random oracle seed `1` passes with `100000` samples.
- [ ] Local release-random CTest passes.
- [ ] Local release-random executable passes with `1000000 --seed 1`.
- [ ] Windows no-waypoint benchmark `--samples 10000 --seed 1` is below the
  average C/oracle ratio threshold `<= 1.5`.
- [ ] Windows waypoint benchmark with fixed seed is recorded as C-only local
  optimizer trend evidence.
- [ ] Local coverage evidence is regenerated under `out/coverage/0.7.0/`.
- [ ] Python prototype smoke passes against the Windows shared build.
- [ ] Rust alpha wrapper smoke passes.
- [ ] Rust alpha examples build under test mode.
- [ ] `original/ruckig-main` diff remains empty.

## Required CI And Manual Gates

- [ ] Release-candidate push CI succeeds.
- [ ] Manual release-random workflow succeeds on the release-candidate commit.
- [ ] Final evidence push CI succeeds.
- [ ] Tag push CI succeeds.
- [ ] Manual release-random workflow succeeds on tag `v0.7.0`.
- [ ] Windows `clang-cl` C-only.
- [ ] Windows `clang-cl` shared C-only.
- [ ] Windows clang oracle.
- [ ] Linux GCC C-only.
- [ ] Linux Clang oracle.
- [ ] macOS Clang C-only.
- [ ] Windows MinGW static consumer.
- [ ] Windows MinGW DLL consumer.
- [ ] Linux ASan/UBSan.
- [ ] Linux Valgrind.
- [ ] Linux no-waypoint performance.
- [ ] Linux waypoint performance.
- [ ] Linux exported symbols.
- [ ] Windows exported symbols.
- [ ] macOS exported symbols.
- [ ] Python prototype smoke on Windows, Linux, and macOS.
- [ ] Rust alpha wrapper smoke.

## Release Closeout

- [ ] Release-candidate implementation commit is created as
  `Release 0.7.0 candidate`.
- [ ] Final evidence commit is created as `Record 0.7.0 release evidence`.
- [ ] Annotated tag `v0.7.0` points to the final evidence commit.
- [ ] GitHub Release `ruckig_c 0.7.0` is published from
  `docs/release/notes/0.7.0.md`.
- [ ] Post-release `main` moves to `0.8.0-design - Unreleased`.
- [ ] `0.7.1` is reserved for emergency patch work only.

## Evidence To Record

```text
Release-candidate commit:
Pending release-candidate commit.

Final release evidence commit and tag target:
Pending final evidence commit.

Annotated tag object:
Pending annotated tag.

Public header diff against v0.6.0:
Pending review after local ABI gates.

Public symbol allowlist:
Pending local ABI gate output.

Exported-symbol evidence:
Pending local and CI exported-symbol gate output.

Tracking CTest result:
Pending local CTest output.

Oracle evidence:
Pending local oracle output.

Performance evidence:
Pending local benchmark output.

Coverage evidence:
Pending local coverage output from out/coverage/0.7.0/.

Wrapper smoke:
Pending Python and Rust smoke output.

CI and manual workflow evidence:
Pending push CI, tag CI, and workflow-dispatch run IDs.
```

## Deferred

- Algorithm visualization and trajectory gallery generation.
- Formal proof of global optimality.
- Formal Ruckig Pro/cloud numerical equivalence.
- Cloud and remote calculation.
- Soft interruption checkpoints.
- Python wheels.
- Rust crate publication.
- Package-manager recipes.
- Upstream baseline upgrade.
