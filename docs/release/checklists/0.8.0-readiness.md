# 0.8.0 Release Readiness Evidence Checklist

This checklist records the focused `0.8.0-readiness` evidence audit on the
`0.8.0-design - Unreleased` line. It is not a stable release closeout, does not
create a tag or GitHub Release, does not bump versions, and does not add public
C API.

## Scope

- [x] Treat current stable release as `v0.7.0`.
- [x] Treat current `main` as `0.8.0-design - Unreleased`.
- [x] Audit the current visualization gallery and verifier evidence for stable
  review readiness.
- [x] Do not add public C API, public symbols, enum values, result-code values,
  or ABI artifact path changes.
- [x] Do not modify CMake, CTest, GitHub Actions workflows, version macros, or
  release notes for a stable release.
- [x] Do not modify the committed 13 PNG gallery assets or
  `docs/assets/visualization/manifest.json`.
- [x] Do not trigger manual release-random workflow.
- [x] Do not create a tag or GitHub Release.
- [x] Keep `original/ruckig-main` frozen.

## Preflight

```text
git -c safe.directory=E:/Yww/DownLoad/source/ruckig_c status --short --branch
## main...ruckig_c/main

git -c safe.directory=E:/Yww/DownLoad/source/ruckig_c log -1 --oneline
2825066 Add 0.8.0 alpha.3 visualization verifier evidence

git -c safe.directory=E:/Yww/DownLoad/source/ruckig_c tag --list "v0.8.0*"
<no output>

git -c safe.directory=E:/Yww/DownLoad/source/ruckig_c ls-remote --tags ruckig_c "refs/tags/v0.8.0*"
<no output>
```

## Focused Local Gates

Build:

```text
cmake --build --preset windows-clang-ninja
ninja: no work to do.

cmake --build --preset windows-clang-ninja-shared
ninja: no work to do.
```

Static routine CTest:

```text
ctest --test-dir out\build\windows-clang-ninja --output-on-failure -E ruckig_c_oracle_random_release
100% tests passed, 0 tests failed out of 42
Total Test time (real) = 50.08 sec
```

Shared routine CTest:

```text
ctest --test-dir out\build\windows-clang-ninja-shared --output-on-failure -E ruckig_c_oracle_random_release
100% tests passed, 0 tests failed out of 42
Total Test time (real) = 46.98 sec
```

Tracking focused CTest:

```text
ctest --test-dir out\build\windows-clang-ninja -R ruckig_c_tracking --output-on-failure
100% tests passed, 0 tests failed out of 11
Total Test time (real) = 44.30 sec
```

Solver branch focused CTest:

```text
ctest --test-dir out\build\windows-clang-ninja -R ruckig_c_solver_branch_coverage --output-on-failure
100% tests passed, 0 tests failed out of 1
Total Test time (real) = 0.01 sec
```

## Visualization Readiness

Syntax:

```text
.\_local\visualization-venv\Scripts\python.exe -m py_compile tools\visualization\generate_gallery.py tools\visualization\verify_gallery.py
passed
```

Default committed-asset verification:

```text
.\_local\visualization-venv\Scripts\python.exe tools\visualization\verify_gallery.py --output docs\assets\visualization
visualization gallery verification passed
assets: 13 PNG files
png_size: 1100x720
manifest_label: 0.8.0-alpha.2 matplotlib visualization evidence
original_examples: 01-10,14-16
excluded_original_examples: 11-13
strict_regenerate: not requested
```

Strict regeneration verification:

```text
$env:RUCKIG_C_SHARED_LIBRARY=(Resolve-Path out\build\windows-clang-ninja-shared\ruckig_c.dll).Path
.\_local\visualization-venv\Scripts\python.exe tools\visualization\verify_gallery.py --output docs\assets\visualization --strict-regenerate
visualization gallery verification passed
assets: 13 PNG files
png_size: 1100x720
manifest_label: 0.8.0-alpha.2 matplotlib visualization evidence
original_examples: 01-10,14-16
excluded_original_examples: 11-13
strict_regenerate: passed
```

The strict regeneration command wrote only ignored temporary artifacts under
`out/`; the committed gallery assets and manifest remain unchanged.

## ABI, Export, And Wrapper Smoke

ABI/export:

```text
cmake --build out\build\windows-clang-ninja-shared --target ruckig_c_verify_public_symbols
ninja: no work to do.

cmake --build out\build\windows-clang-ninja-shared --target ruckig_c_compare_public_exported_symbols
ninja: no work to do.
```

Python prototype smoke:

```text
$env:RUCKIG_C_SHARED_LIBRARY=(Resolve-Path out\build\windows-clang-ninja-shared\ruckig_c.dll).Path
python bindings\python_prototype\test_prototype.py
.....................
----------------------------------------------------------------------
Ran 21 tests in 0.034s

OK
```

Rust alpha wrapper smoke:

```text
cargo test --manifest-path bindings\rust\Cargo.toml
running 13 tests
test result: ok. 13 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out
Doc-tests ruckig_c_alpha
test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out

cargo test --manifest-path bindings\rust\Cargo.toml --examples
examples offline, per_section_minimum_duration, position, tracking,
tracking_optimized, velocity, and waypoints all ran with 0 test failures.
```

## Boundary Checks

Public API, ABI, CI, and CMake boundary:

```text
git -c safe.directory=E:/Yww/DownLoad/source/ruckig_c diff -- include/ruckig_c/ruckig.h docs/abi/public-symbols.txt docs/abi/public-symbol-exceptions.txt docs/abi/exceptions.md .github/workflows/ci.yml CMakeLists.txt
<no output>
```

Committed gallery assets unchanged:

```text
git -c safe.directory=E:/Yww/DownLoad/source/ruckig_c diff -- docs/assets/visualization
<no output>
```

Frozen original confirmation:

```text
git -c safe.directory=E:/Yww/DownLoad/source/ruckig_c diff -- original/ruckig-main
<no output>
```

Diff check:

```text
git -c safe.directory=E:/Yww/DownLoad/source/ruckig_c diff --check
exit code 0
```

## Remote Verification

This is a single readiness evidence commit. Ordinary GitHub Actions push CI is
confirmed after the commit is pushed; the run URL and result are recorded in the
final operator summary rather than self-referentially embedded in this same
commit.

Manual release-random workflow is not triggered. No tag or GitHub Release is
created.

## Readiness Conclusion

Local focused readiness gates are clean for entering a later `v0.8.0` stable
closeout decision. If ordinary push CI for this readiness evidence commit is
green, the next separate project step can be `v0.8.0` stable closeout.

The stable closeout remains a separate decision because it would perform
version/CMake macro updates, release notes/checklist finalization, CI
confirmation, annotated tag creation, GitHub Release publication, and
post-release transition work.

## Deferred

- Stable `v0.8.0` version bump, tag, and GitHub Release.
- Manual release-random workflow.
- Coverage, performance, and 1M release-random evidence.
- Default CI plotting or verifier job.
- CMake/CTest integration for visualization verification.
- Optional CI artifact upload for gallery PNGs.
- PDF/SVG export.
- Stable Python package publication.
- Rust crate publication.
- Package-manager recipes.
- Cloud/remote calculation.
- Formal Pro/cloud numerical equivalence claims.
- Formal global optimality proof.
- Upstream baseline upgrade.
