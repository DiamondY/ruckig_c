# Ruckig C 0.3.0 Release Checklist

This checklist records the `v0.3.0` hardening-release closeout gates and
evidence. `0.3.0` does not add public C API, does not change solver scope, does
not publish bindings, does not add package-manager recipes, and does not update
the frozen upstream oracle baseline.

## Scope

- [ ] Final release commit hash recorded.
- [x] Version is `0.3.0` in `CMakeLists.txt` if a `v0.3.0` tag is being cut.
- [x] Version macros in `include/ruckig_c/ruckig.h` are `0.3.0` if a
  `v0.3.0` tag is being cut.
- [x] `CHANGELOG.md` has a dated `0.3.0` release entry.
- [x] `docs/design/0.3.0_release_decision.md` records `0.3.0` as a hardening
  release.
- [x] Public header diff reviewed against `v0.2.5`.
- [x] Public header diff contains only approved version macro or documentation
  changes.
- [x] No public C function additions, removals, or signature changes.
- [x] No `RUCKIG_C_API` public symbol additions.
- [x] Result-code and enum numeric values unchanged.
- [x] `docs/abi/public-symbols.txt` unchanged unless separately approved.
- [x] `docs/abi/public-symbol-exceptions.txt` remains empty.
- [x] Strict Linux/Windows public exported-symbol comparison remains
  trial/evidence mode, not routine CI hard failure.
- [ ] macOS exported-symbol evidence is recorded as a `0.3.0` baseline
  bootstrap, not a historical `v0.2.5` diff.
- [x] Intermediate waypoints remain unsupported.
- [x] Per-section constraints remain unsupported.
- [x] Cloud calculation remains unsupported.
- [x] Python prototype remains feasibility evidence only: not installed, not
  published, not packaged, and not routine CI.
- [x] Rust bindings remain deferred.
- [x] Package-manager recipes and new package-manager prototypes remain frozen.
- [x] MSVC `cl` static/DLL consumer smokes remain optional local gates, not
  routine CI.
- [x] `original/ruckig-main` remains frozen as the Ruckig Community `0.17.3`
  oracle baseline.

## Required Gates

- [x] `git status --short --ignored` is clean before release closeout edits.
- [x] Windows default preset configures, builds, and tests successfully:
  `cmake --preset windows-clang-ninja`,
  `cmake --build --preset windows-clang-ninja`,
  `ctest --preset windows-clang-ninja`.
- [x] Windows shared preset configures, builds, and tests successfully:
  `cmake --preset windows-clang-ninja-shared`,
  `cmake --build --preset windows-clang-ninja-shared`,
  `ctest --preset windows-clang-ninja-shared`.
- [x] `ruckig_c_verify_public_symbols` passes.
- [x] `ruckig_c_compare_public_exported_symbols` produces clean
  warning/evidence output locally.
- [x] Fixed C++ oracle suite passes and reports the final case count.
- [x] Development random oracle passes.
- [ ] Manual release random oracle passes with `release_random=true`.
- [ ] Linux performance benchmark average ratio is `<= 1.5`.
- [x] GitHub Actions push CI passes for the release evidence commit.
- [x] Linux Clang ASan+UBSan passes.
- [x] Linux Valgrind passes.
- [x] Installed CMake consumer smoke test passes.
- [x] Unix pkg-config consumer smoke test passes where `pkg-config` is
  available.
- [x] Unix shared install-tree consumer test passes where `pkg-config` is
  available.
- [x] Windows clang-cl static C-only path passes in CI.
- [x] Windows clang-cl shared C-only path passes in CI.
- [x] Windows clang oracle path passes in CI.
- [x] Windows MinGW static consumer passes in routine CI.
- [x] Windows MinGW DLL consumer passes in routine CI.
- [x] MSVC `cl` standalone static smoke remains locally verified or explicitly
  documented as optional local evidence.
- [x] MSVC `cl` standalone DLL smoke remains locally verified or explicitly
  documented as optional local evidence.
- [ ] Linux exported-symbol artifact uploaded and reviewed.
- [ ] Windows exported-symbol artifact uploaded and reviewed.
- [ ] macOS exported-symbol artifact uploaded and reviewed as bootstrap
  evidence.
- [x] Python prototype smoke passes with `RUCKIG_C_SHARED_LIBRARY` pointing to
  the Windows shared build.
- [x] `scripts/clean-local.ps1` dry run reviewed.
- [x] `scripts/clean-local.ps1 -Apply` removes only ignored local artifacts.
- [ ] Final `git status --short --ignored` contains only expected tracked
  release-preparation files and ignored artifacts required for evidence, or is
  clean after cleanup.

## Evidence To Record

```text
Final release commit:
TBD.
Pre-release local status:
Clean before release-preparation edits at HEAD `cf956eb`.
Push CI run id:
27023713278.
Push CI URL:
https://github.com/DiamondY/ruckig_c/actions/runs/27023713278.
Push CI conclusion:
success.
Manual release random run id:
Blocked: current GitHub CLI token is invalid, so `gh workflow run ci.yml --ref main -f release_random=true` returns HTTP 401.
Manual release random URL:
Blocked pending GitHub CLI re-authentication.
Manual release random conclusion:
Blocked pending GitHub CLI re-authentication.
Windows default CTest result:
100% tests passed, 0 tests failed out of 15.
Windows shared CTest result:
100% tests passed, 0 tests failed out of 15.
Fixed oracle result:
Oracle comparisons passed: 76.
Development random oracle result:
Oracle comparisons passed: 76; Random oracle comparisons passed: 100000 seed 1.
Public symbol allowlist result:
Clean; 66 header public symbols match 66 expected public symbols.
Public exported-symbol comparison result:
Windows local public exported-symbol comparison clean; strict_public_abi OFF; 0 missing, 0 added, 0 removed, 0 unapproved exported symbols.
Linux exported-symbol artifact:
Uploaded by push CI as artifact id 7440256570, size 3372 bytes, digest sha256:3366cbf00d71641d1177ce71fb964cf39ed92da06f2c0898e63ae66471f9da86. Download/review blocked by invalid GitHub CLI token.
Windows exported-symbol artifact:
Uploaded by push CI as artifact id 7440259690, size 2969 bytes, digest sha256:64cdc30e82e74ef18536b7e2a9e9f9831042bd5e7f2789e03538c50ea2a2dea8. Download/review blocked by invalid GitHub CLI token.
macOS exported-symbol artifact:
Uploaded by push CI as artifact id 7440252482, size 1838 bytes, digest sha256:28e0b8dad01133c2c36a2716c0a6f79f9c0f0d43eb9fbcb07d7b66add0d6bdd1. Download/review blocked by invalid GitHub CLI token.
macOS baseline decision:
0.3.0 records the first macOS shared/export snapshot as bootstrap evidence;
no v0.2.5 macOS historical baseline exists.
Linux performance result:
Uploaded by push CI as artifact id 7440260459, size 526 bytes, digest sha256:a0ce224771935197c9d02317cb385a81d9accf631735793947d5228883649993. Ratio review blocked by invalid GitHub CLI token.
Python prototype smoke:
Ran 8 tests in 0.005s, OK.
Consumer automation result:
Push CI passed Windows clang-cl static/shared C-only, Windows clang oracle, Linux GCC C-only, Linux Clang oracle, macOS C-only, Windows MinGW static, Windows MinGW DLL, Linux ASan/UBSan, Linux Valgrind, Linux exported symbols, Windows exported symbols, macOS exported symbols, and Linux performance jobs.
MSVC cl standalone consumer result:
Locally verified optional local gates; not routine CI.
Package-manager result:
No package-manager recipe or new prototype added.
Public header diff review result:
Diff against `v0.2.5` contains only version macro changes from `0.2.5` to `0.3.0`.
Enum/result-code review result:
Enum and result-code numeric values unchanged.
GitHub Release URL:
TBD.
```

## ABI Review Commands

```powershell
git -c safe.directory=E:/Yww/DownLoad/source/ruckig_c diff v0.2.5 -- include/ruckig_c/ruckig.h
cmake --build out\build\windows-clang-ninja-shared --target ruckig_c_verify_public_symbols
cmake --build out\build\windows-clang-ninja-shared --target ruckig_c_compare_public_exported_symbols
```

The public exported-symbol comparison remains warning/evidence-only for
`0.3.0`. Do not promote it to routine CI hard failure in this release.

## Release Commands

Run these only after all required gates are complete and evidence is recorded:

```powershell
git -c safe.directory=E:/Yww/DownLoad/source/ruckig_c tag -a v0.3.0 -m "ruckig_c 0.3.0"
git -c safe.directory=E:/Yww/DownLoad/source/ruckig_c push origin v0.3.0
```

Create the GitHub Release only after confirming the annotated tag points at the
final release closeout commit.
