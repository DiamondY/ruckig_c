# 0.15.0-alpha.3 Consumer And Wrapper Interrupt Smoke

Status: local gates passed; committed as local alpha evidence.

This slice adds user-facing smoke coverage for the interrupt behavior stabilized
in `v0.14.0`. It keeps wrappers prototype-only and does not change the public C
API, public ABI, version metadata, tag/release state, remote CI state, or
manual workflows.

## Scope

- [x] Added C example `examples/c/21_no_waypoint_interrupt_boundary.c`.
- [x] Added C example `examples/c/22_tracking_interrupt_boundary.c`.
- [x] Wired both examples into CMake and added focused `ruckig_c_examples_*`
  CTest names for the alpha.3 gate selector.
- [x] Added Python prototype smoke tests for no-waypoint interruption and
  Optimized tracking update/lookahead interruption.
- [x] Added Rust wrapper smoke tests for no-waypoint interruption and Optimized
  tracking update/lookahead interruption.
- [x] Added Rust examples `interrupt_no_waypoint.rs` and
  `interrupt_tracking.rs`.

## Boundaries

- [x] No change to `include/ruckig_c/ruckig.h`.
- [x] No new public functions, public structs, exported symbols, enum values,
  result-code values, ABI allowlist entries, public diagnostics fields, or
  runtime clock public hooks.
- [x] Wrappers remain prototype-only: no wheel, crate publication, stable
  wrapper API commitment, or package-manager recipe.
- [x] No push, tag, GitHub Release, version bump, or manual workflow.

## Local Gates

| Gate | Result |
| --- | --- |
| `cmake --build --preset windows-clang-ninja` | Passed |
| `ctest --test-dir out\build\windows-clang-ninja --output-on-failure -R "ruckig_c_examples\|ruckig_c_interrupt_post_release_quality\|ruckig_c_no_waypoint_interrupt_audit\|ruckig_c_tracking_interrupt_audit"` | Passed, 5/5 |
| `python bindings\python_prototype\test_prototype.py` with `RUCKIG_C_SHARED_LIBRARY` pointed at the shared DLL | Passed, 23/23 |
| `cargo test --manifest-path bindings\rust\Cargo.toml` | Passed, 15/15 |
| `cargo test --manifest-path bindings\rust\Cargo.toml --examples` | Passed |
| `cmake --build --preset windows-clang-ninja-shared` | Passed |
| `cmake --build out\build\windows-clang-ninja-shared --target ruckig_c_verify_public_symbols` | Passed |
| `cmake --build out\build\windows-clang-ninja-shared --target ruckig_c_compare_public_exported_symbols` | Passed; public symbols `172`, additions `0`, removals `0`, unapproved exports `0` |
| Final boundary checks | Passed; public header, ABI docs, workflow, `original/ruckig-main`, and visualization assets unchanged |

## Deferred

- Tracking sequence interruption remains deferred until a separate public
  diagnostics/API decision is accepted.
- Formal Python/Rust API publication, package-manager recipes, Cloud/Pro
  claims, hard real-time guarantees, formal global optimality proof, upstream
  baseline upgrade, and runtime clock public hooks remain deferred.
