# 0.17.0 Wrapper Package Policy Design Checklist

This checklist records the docs-only wrapper package policy design slice. It
does not implement wrapper stabilization, publish packages, change the public C
ABI, add package-manager recipes, or change release state.

## Scope

- [x] Added `docs/design/0.17.0_wrapper_package_policy.md`.
- [x] Kept Python and Rust wrappers prototype-only.
- [x] Did not choose Python-first, Rust-first, or dual-wrapper implementation.
- [x] Did not change C, Python, or Rust implementation files.
- [x] Did not publish a Python wheel, Rust crate, source distribution, or
  package-manager recipe.

## Policy Topics

- [x] Recorded Windows DLL, Linux shared object, and macOS dylib discovery
  questions.
- [x] Compared external installed shared library, vendored shared library, and
  static linking strategies.
- [x] Recorded Python `cffi` ABI-mode prototype implications.
- [x] Recorded Rust alpha wrapper linking/build policy gaps.
- [x] Recorded C ABI version compatibility and unsupported-version behavior as
  required future decisions.
- [x] Recorded package artifact ownership, CI matrix, and release checklist
  ownership as blockers.
- [x] Listed the only later allowed routes: Python-first, Rust-first,
  dual-wrapper, or continued prototype-only.

## Boundaries

- [x] No public C header change.
- [x] No public symbol allowlist or exception change.
- [x] No workflow, version metadata, tag, or GitHub Release change.
- [x] No update to `original/ruckig-main`.
- [x] No visualization asset change.
- [x] No default heavy random, coverage, performance, or static analyzer CI
  gate change.

## Verification

| Gate | Result |
| --- | --- |
| `cmake --build --preset windows-clang-ninja` | Passed; no work to do |
| `ctest --test-dir out\build\windows-clang-ninja --output-on-failure -R "ruckig_c_header_c\|ruckig_c_header_cpp\|ruckig_c_public_diagnostics"` | Passed; 3/3 tests |
| Public header / ABI / workflow boundary diff | Empty |
| `original/ruckig-main` and visualization asset boundary diff | Empty |
| `git diff --check` | Passed with CRLF normalization warnings only |
