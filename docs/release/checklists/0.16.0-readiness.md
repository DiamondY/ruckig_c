# 0.16.0 Public Diagnostics Readiness Checklist

Status: local readiness evidence complete; ordinary remote push CI is observed
after the checklist commit is pushed.

This checklist closes the current public diagnostics design-line evidence
without publishing `v0.16.0`, creating a tag, changing version metadata,
starting wrapper stabilization, changing workflows, or editing package-manager
recipes.

## Scope

- [x] Recorded final public diagnostics ABI delta.
- [x] Recorded compatibility policy for existing result codes, existing public
  struct layouts, and legacy API behavior.
- [x] Recorded wrapper status as prototype-only.
- [x] Recorded deferred release and tooling items.
- [x] Updated current diagnostics, quality, coverage, roadmap, index, and
  release evidence documents.

## Public ABI Readiness

- [x] Stable baseline: `v0.15.0`, 184 public symbols.
- [x] Alpha.3 added 4 symbols:
  `ruckig_diagnostics_init`,
  `ruckig_validate_input_with_diagnostics`,
  `ruckig_calculate_with_diagnostics`, and
  `ruckig_update_with_diagnostics`.
- [x] Alpha.5 added 2 symbols:
  `ruckig_tracking_get_last_public_diagnostics` and
  `ruckig_tracking_sequence_continuation_get_last_diagnostics`.
- [x] Expected design-line public symbol count after alpha.5: 190.
- [x] Existing `ruckig_result_t` numeric values are unchanged.
- [x] Existing public struct layouts are unchanged.
- [x] `ruckig_diagnostics_t` is opt-in and caller-owned.
- [x] Legacy APIs keep behavior and return-code compatibility.

## Local Gates

| Gate | Result |
| --- | --- |
| `cmake --build --preset windows-clang-ninja` | Pass; no work to do after alpha.5 |
| `cmake --build --preset windows-clang-ninja-shared` | Pass; no work to do after alpha.5 |
| `cmake --build --preset windows-clang-ninja-oracle` | Pass |
| `cmake --build --preset windows-clang-ninja-performance` | Pass |
| `ctest --test-dir out\build\windows-clang-ninja --output-on-failure` | Pass; 69/69 tests passed |
| `ctest --test-dir out\build\windows-clang-ninja-shared --output-on-failure` | Pass; 69/69 tests passed |
| `out\build\windows-clang-ninja-oracle\ruckig_c_oracle_tests.exe` | Pass; fixed oracle 92, waypoint section oracle 4 |
| `out\build\windows-clang-ninja-oracle\ruckig_c_oracle_tests.exe --random 100000 --seed 1` | Pass |
| `out\build\windows-clang-ninja-oracle\ruckig_c_oracle_tests.exe --random 100000 --seed 2` | Pass |
| `out\build\windows-clang-ninja-oracle\ruckig_c_oracle_tests.exe --random 100000 --seed 41` | Pass |
| `out\build\windows-clang-ninja-oracle\ruckig_c_oracle_tests.exe --random-per-dof 100000 --seed 1` | Pass |
| `out\build\windows-clang-ninja-performance\ruckig_c_performance_benchmark.exe --samples 10000 --seed 1 --enforce-threshold` | Pass; average ratio `1.29332` <= `1.5` |
| `out\build\windows-clang-ninja-performance\ruckig_c_performance_benchmark.exe --samples 10000 --seed 1 --waypoints` | Pass; alpha C-only waypoint corpus |
| `cmake --build out\build\windows-clang-ninja-shared --target ruckig_c_verify_public_symbols` | Pass |
| `cmake --build out\build\windows-clang-ninja-shared --target ruckig_c_compare_public_exported_symbols` | Pass; 190 approved/current public symbols, 0 missing public symbols, 0 unapproved exported symbols |
| `cargo test --manifest-path bindings\rust\Cargo.toml` | Pass; 16 tests |
| `cargo test --manifest-path bindings\rust\Cargo.toml --examples` | Pass |
| `$env:RUCKIG_C_SHARED_LIBRARY='E:\Yww\DownLoad\source\ruckig_c\out\build\windows-clang-ninja-shared\ruckig_c.dll'; python bindings\python_prototype\test_prototype.py` | Pass; 24 tests |

## Boundary Checks

| Check | Result |
| --- | --- |
| `git diff -- docs/abi/public-symbol-exceptions.txt docs/abi/exceptions.md .github/workflows/ci.yml` | Clean |
| `git diff -- original/ruckig-main docs/assets/visualization` | Clean |
| `git diff --check` | Pass; Git reported expected CRLF normalization warnings only |

## Deferred

- [x] Stable `v0.16.0` version bump, release tag, and GitHub Release remain
  deferred.
- [x] Wrapper stabilization remains deferred; Python/Rust wrappers stay
  prototype-only smoke targets.
- [x] Package-manager recipes remain frozen.
- [x] Generated fixture auto-write remains deferred.
- [x] Tracking failure shrinker remains deferred.
- [x] Solver long-tail coverage remains deferred unless future cases are
  oracle-backed or public-behavior-backed.

## Remote CI

Ordinary remote push CI is observed after pushing this readiness commit to
`main`. The run URL and conclusion should be recorded in the delivery summary.
