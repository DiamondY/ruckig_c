# Ruckig C 0.6.0 Release Checklist

This checklist records the stable `v0.6.0` Optimized tracking release
closeout. The release stabilizes the bounded local `RUCKIG_TRACKING_OPTIMIZED`
MVP and the public C Optimized tracking API added on the `0.6.0-design` line.
It does not claim formal global optimality, source-level tracking oracle
parity, or Pro/cloud numerical equivalence.

## Scope

- [x] Version is `0.6.0` in `CMakeLists.txt`.
- [x] Version macros in `include/ruckig_c/ruckig.h` are `0.6.0`.
- [x] `CHANGELOG.md` has a dated `0.6.0` release entry.
- [x] `README.md` describes `v0.6.0` as the current stable release.
- [x] `docs/current/roadmap.md` records `v0.6.0` as the stable Optimized
  tracking release and `0.7.0-design` as the next tracking deepening line.
- [x] Existing `v0.5.0` public functions are not removed or signature-changed.
- [x] Existing enum numeric values and result-code numeric values are
  unchanged.
- [x] The public C Optimized tracking API is stabilized in `v0.6.0`.
- [x] Local Fast-mode online/offline tracking behavior remains included.
- [x] Local bounded Optimized online/offline tracking behavior is included.
- [x] Python `cffi` remains prototype-only; no wheel is published.
- [x] Rust remains an alpha wrapper; no crate is published.
- [x] Package-manager recipes remain deferred.
- [x] No Pro/cloud sample, network dependency, or external license is used as a
  release gate.
- [x] `original/ruckig-main` remains frozen as Ruckig Community `0.17.3`.

## Public C ABI Review

- [x] `include/ruckig_c/ruckig.h` contains the approved Optimized tracking
  public C ABI and no additional closeout-only public API expansion.
- [x] `docs/abi/public-symbols.txt` contains the 169-symbol public allowlist.
- [x] `docs/abi/public-symbol-exceptions.txt` records the approved Optimized
  tracking public additions from the `0.6.0-design` line.
- [x] ABI artifact paths use `artifacts/abi/0.6.0`.
- [x] Public header diff against `v0.5.0` is reviewed:

  ```powershell
  git -c safe.directory=E:/Yww/DownLoad/source/ruckig_c diff v0.5.0 -- include/ruckig_c/ruckig.h
  ```

- [x] Public header diff contains only the approved Optimized tracking enum,
  diagnostic/lookahead function declarations, version macros, and release
  scope comments.
- [x] Public symbol allowlist verification passes.
- [x] Public exported-symbol comparison passes.
- [x] Current public symbols: `169`.
- [x] Public removals: `0`.
- [x] Unapproved exported symbols: `0`.
- [x] Tracking internals are not exported.

## Tracking Release Evidence

- [x] C tracking tests cover API lifecycle, validation, online Fast update,
  deterministic fixed corpus, offline sequence calculation, Optimized
  single-target update, online lookahead, offline sliding-window calculation,
  fallback diagnostics, quality smoke, and no-allocation paths.
- [x] C tracking examples cover online Fast tracking, offline sequence
  tracking, online Optimized lookahead, online Optimized sinus, and offline
  Optimized sequence.
- [x] `Optimized` mode is implemented as a bounded deterministic local MVP.
- [x] `Optimized` mode uses Fast fallback when no feasible candidate improves
  the baseline; fallback is observable through public diagnostics.
- [x] `Optimized` mode does not claim formal global optimality or Pro/cloud
  numerical equivalence.
- [x] Sinus tracking remains trend-only and is not a hard release gate.
- [x] `interrupt_calculation_duration` does not create tracking timeout,
  soft-interruption, or hard real-time guarantees.
- [x] Python prototype smoke covers Optimized mode, online lookahead,
  candidate-budget diagnostics, fallback/status behavior, and lifecycle checks.
- [x] Rust alpha smoke covers Optimized mode, online lookahead,
  candidate-budget diagnostics, fallback/status behavior, and examples.

## Required Local Gates

- [x] Static Windows clang/Ninja build passes.
- [x] Shared Windows clang/Ninja build passes.
- [x] Performance Windows clang/Ninja build passes.
- [x] Static CTest without release random passes.
- [x] Shared CTest without release random passes.
- [x] Tracking CTest gates pass.
- [x] Fixed oracle suite passes.
- [x] Ordinary random oracle seeds `1`, `2`, and `41` pass with `100000`
  samples each.
- [x] Per-DoF random oracle seed `1` passes with `100000` samples.
- [x] Local release-random CTest passes.
- [x] Local release-random executable passes with `1000000 --seed 1`.
- [x] Windows no-waypoint benchmark `--samples 10000 --seed 1` is below the
  average C/oracle ratio threshold `<= 1.5`.
- [x] Windows waypoint benchmark with fixed seed is recorded as C-only local
  optimizer trend evidence.
- [x] Python prototype smoke passes against the Windows shared build.
- [x] Rust alpha wrapper smoke passes.
- [x] Rust alpha examples build under test mode.

## Required CI And Manual Gates

- [ ] Release-candidate push CI succeeds.
- [ ] Manual release-random workflow succeeds on the release-candidate commit.
- [ ] Final evidence push CI succeeds.
- [ ] Tag push CI succeeds.
- [ ] Manual release-random workflow succeeds on tag `v0.6.0`.
- [ ] Windows `clang-cl` C-only.
- [ ] Windows `clang-cl` shared C-only.
- [ ] Windows clang oracle.
- [ ] Linux GCC C-only.
- [ ] Linux Clang oracle.
- [ ] macOS Clang C-only.
- [ ] Windows MinGW static consumer.
- [ ] Windows MinGW DLL consumer.
- [ ] Linux ASan/UBSan.
- [ ] Linux Valgrind.
- [ ] Linux no-waypoint performance.
- [ ] Linux waypoint performance.
- [ ] Linux exported symbols.
- [ ] Windows exported symbols.
- [ ] macOS exported symbols.
- [ ] Python prototype smoke on Windows, Linux, and macOS.
- [ ] Rust alpha wrapper smoke.

## Release Closeout

- [ ] Release-candidate implementation commit is created as
  `Release 0.6.0 candidate`.
- [ ] Final evidence commit is created as `Record 0.6.0 release evidence`.
- [ ] Annotated tag `v0.6.0` points to the final evidence commit.
- [ ] GitHub Release `ruckig_c 0.6.0` is published from
  `docs/release/notes/0.6.0.md`.
- [ ] Post-release `main` moves to `0.7.0-design - Unreleased`.
- [ ] `0.6.1` is reserved for emergency patch work only.

## Evidence To Record

```text
Release-candidate commit:
TBD.

Final release evidence commit and tag target:
TBD.

Annotated tag object:
TBD.

Public header diff against v0.5.0:
Reviewed. Additions are limited to version macros, the approved
`ruckig_tracking_calculation_status_t` enum, approved Optimized tracking
diagnostic/lookahead public functions, and release scope comments. No existing
`v0.5.0` public symbol is removed or signature-changed.

Public symbol allowlist:
169 public symbols. Header-derived public symbols match
docs/abi/public-symbols.txt with 0 missing and 0 extra.

Windows exported-symbol evidence:
Public exported-symbol comparison is clean. Current exports: 169; approved
public symbols: 169; missing public symbols: 0; removed public symbols: 0;
unapproved exported symbols: 0; exception-approved additions since the v0.2.5
historical baseline: 103.

Linux exported-symbol evidence:
TBD.

macOS exported-symbol evidence:
TBD.

Tracking CTest result:
8/8 tracking CTest gates passed locally.

Fixed oracle result:
Oracle comparisons passed: 76.

Waypoint section oracle result:
Waypoint section oracle comparisons passed: 4.

Random oracle result:
100000 seed 1: pass.
100000 seed 2: pass.
100000 seed 41: pass.

Random per-DoF result:
100000 seed 1: pass.

Local release random CTest:
`ruckig_c_oracle_random_release` passed locally in 56.70 seconds.

Local release random executable:
1000000 seed 1: pass.

Release random result before tag:
TBD.

Windows no-waypoint performance:
average_ratio_c_over_oracle 1.28367; threshold 1.5. An earlier same-command
local run measured 1.50053 and was discarded as workstation noise after the
repeat run passed the release threshold.

Windows waypoint performance:
Windows waypoint benchmark samples: 10000; seed: 1; waypoint_case_count: 10;
waypoint_max_dofs: 8; waypoint_max_intermediate_positions: 3;
waypoint_c_average_ns: 3.26727e+06; waypoint_c_p99_ns: 1.20425e+07;
waypoint_c_worst_ns: 1.75884e+07; waypoint_oracle_ratio unavailable by policy.

Python prototype:
20/20 tests passed locally against the Windows shared build.

Rust alpha wrapper:
12/12 unit tests passed locally. Examples compiled under `cargo test
--examples`.

CI:
TBD.

GitHub Release:
TBD.

Deferred:
Pro/cloud numerical equivalence, cloud/remote calculation, soft interruption
checkpoints, formal Python/Rust publication, package-manager recipes, and
upstream baseline upgrade.
```
