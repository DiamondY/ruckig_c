# 0.9.0-alpha.3 Tracking Stability Evidence Checklist

This checklist records the `0.9.0-alpha.3` tracking stability regression
evidence on `0.9.0-design - Unreleased`. It is not a stable release closeout
and does not create a tag or GitHub Release.

## Scope

- [x] Add `ruckig_c_tests --tracking-stability`.
- [x] Add routine CTest `ruckig_c_tracking_stability`.
- [x] Freeze representative alpha.2 tuned behavior as fixed regression cases.
- [x] Cover optimized strict selection, Aggressive near-tie selection, Fast
  fallback, budget exhaustion, disabled DoF, `tight_valid` constraints, all
  strategies, all signals, all lookahead buckets, and all selected candidate
  families currently exposed by alpha.2.
- [x] Keep evaluator scoring, candidate generation, strategy weights, near-tie
  policy, public diagnostics structs, and public C ABI unchanged.
- [x] Keep existing `--tracking-random` behavior and output unchanged.
- [x] Keep Python/Rust prototype-only; no wheel or crate publication.
- [x] Keep package-manager recipes, visualization v2, soft interruption,
  cloud/remote calculation, Pro/cloud equivalence, formal global optimality
  proof, and upstream baseline upgrade deferred.
- [x] Keep `original/ruckig-main` frozen.

## Stability Evidence

Focused selector:

```text
out\build\windows-clang-ninja\ruckig_c_tests.exe --tracking-stability
17 stability cases passed.
selected families covered: fast, instantaneous, horizon, terminal_blend, derivative_damped, lead_lag.
selected near-tie case: seed 1 sample 1400, Aggressive, instantaneous, budget_exhausted 6, improvement 0.849844951.
```

Focused CTest:

```text
ctest --test-dir out\build\windows-clang-ninja -R ruckig_c_tracking_stability --output-on-failure
1/1 test passed in 0.02 seconds.

ctest --test-dir out\build\windows-clang-ninja -R ruckig_c_tracking --output-on-failure
14/14 tests passed in 46.35 seconds.
```

## Local Gates

- [x] Static Windows clang/Ninja build passes.
- [x] Shared Windows clang/Ninja build passes.
- [x] Performance Windows clang/Ninja build passes.
- [x] Static CTest without release random passes.
- [x] Shared CTest without release random passes.
- [x] Tracking CTest gates pass, including the new stability CTest.
- [x] Focused `ruckig_c_tracking_stability` CTest passes.
- [x] Alpha.2 10k/100k/1M audit thresholds remain passing.
- [x] No-waypoint performance benchmark remains under threshold.
- [x] ABI public symbol allowlist verification passes.
- [x] Public exported-symbol comparison passes.
- [x] Python prototype smoke passes.
- [x] Rust alpha wrapper smoke passes.
- [x] Rust examples pass.
- [x] Public ABI/CI/header boundary diff remains empty.
- [x] `original/ruckig-main` diff remains empty.
- [x] `git diff --check` passes.

Builds:

```text
cmake --build --preset windows-clang-ninja
ninja: no work to do.

cmake --build --preset windows-clang-ninja-shared
3/3 build steps completed after CMake regeneration.

cmake --build --preset windows-clang-ninja-performance
ninja: no work to do.
```

Routine CTest:

```text
ctest --test-dir out\build\windows-clang-ninja --output-on-failure -E ruckig_c_oracle_random_release
45/45 tests passed in 48.45 seconds.

ctest --test-dir out\build\windows-clang-ninja-shared --output-on-failure -E ruckig_c_oracle_random_release
45/45 tests passed in 48.51 seconds.
```

Alpha.2 threshold regression:

```text
10k seed 1: stable PASS, balanced PASS, aggressive PASS.
100k seed 1: stable PASS, balanced PASS, aggressive PASS.
100k seed 2: stable PASS, balanced PASS, aggressive PASS.
100k seed 41: stable PASS, balanced PASS, aggressive PASS.
1M seed 1: stable PASS, balanced PASS, aggressive PASS.
```

Representative threshold values:

```text
100k seed 1 aggressive: optimized 33310 required 3217 average_improvement 0.00946028786669 required 0.007933984795 PASS
100k seed 2 aggressive: optimized 33259 required 3158 average_improvement 0.00874542449754 required 0.007070771532 PASS
100k seed 41 aggressive: optimized 33302 required 3124 average_improvement 0.00875016454004 required 0.007632562487 PASS
1M seed 1 aggressive: optimized 332552 required 31635 average_improvement 0.00932603685594 required 0.007832339636 PASS
```

Performance:

```text
out\build\windows-clang-ninja-performance\ruckig_c_performance_benchmark.exe --samples 10000 --seed 1
samples: 10000
seed: 1
c_average_ns: 664.57
c_p99_ns: 4900
c_worst_ns: 16200
oracle_average_ns: 573.29
oracle_p99_ns: 4300
oracle_worst_ns: 25900
average_ratio_c_over_oracle: 1.15922
release_threshold_average_ratio: 1.5
```

ABI/export:

```text
cmake --build out\build\windows-clang-ninja-shared --target ruckig_c_verify_public_symbols
ninja: no work to do.

cmake --build out\build\windows-clang-ninja-shared --target ruckig_c_compare_public_exported_symbols
ninja: no work to do.
```

Expected unchanged ABI result: public symbol count remains `172`, public
additions in `0.9.0-alpha.3` are `0`, public removals are `0`, and unapproved
exported symbols are `0`.

Wrapper smoke:

```text
python bindings\python_prototype\test_prototype.py
Ran 21 tests in 0.032s
OK

cargo test --manifest-path bindings\rust\Cargo.toml
13 passed; 0 failed

cargo test --manifest-path bindings\rust\Cargo.toml --examples
example test harnesses passed
```

Boundary checks:

```text
git -c safe.directory=E:/Yww/DownLoad/source/ruckig_c diff -- include/ruckig_c/ruckig.h docs/abi/public-symbols.txt docs/abi/public-symbol-exceptions.txt docs/abi/exceptions.md .github/workflows/ci.yml
<no output>

git -c safe.directory=E:/Yww/DownLoad/source/ruckig_c diff -- original/ruckig-main
<no output>

git -c safe.directory=E:/Yww/DownLoad/source/ruckig_c diff --check
exit code 0
```

The `diff --check` command printed only Git line-ending normalization warnings
for touched text files.

## Remote Evidence

- [ ] Ordinary push CI succeeds for the evidence commit.
- [x] Manual release-random workflow is not triggered.
- [x] No tag is created.
- [x] No GitHub Release is created.

This is a single evidence commit. Ordinary GitHub Actions push CI is confirmed
after the commit is pushed; the run URL and result are recorded in the final
operator summary rather than self-referentially embedded in this same commit.

## Deferred

- `0.9.0-readiness` audit.
- Stable `v0.9.0` release closeout.
- Public C API or ABI expansion.
- Default CI plotting or visualization v2.
- Stable Python package publication.
- Rust crate publication.
- Package-manager recipes.
- Cloud/remote calculation.
- Formal Pro/cloud numerical equivalence claims.
- Formal global optimality proof.
- Soft interruption checkpoints.
- Upstream baseline upgrade.
