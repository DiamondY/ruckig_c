# Ruckig C 0.10.0 Release Checklist

This checklist records the stable `v0.10.0` Visualization v2 evidence release
closeout. The release keeps the `v0.9.0` 172-symbol public C ABI unchanged
while adopting the Visualization v2 gallery, local verifier, strict
regeneration, and manual-only CI artifact evidence reviewed during
`0.10.0-readiness`.

## Scope

- [x] Version is `0.10.0` in `CMakeLists.txt`.
- [x] Version macros in `include/ruckig_c/ruckig.h` are `0.10.0`.
- [x] `CHANGELOG.md` has a dated `0.10.0` release entry.
- [x] `README.md` describes `v0.10.0` as the current stable release.
- [x] `docs/current/roadmap.md` records `v0.10.0` as the stable Visualization
  v2 evidence release.
- [x] Existing `v0.9.0` public functions are not removed or signature-changed.
- [x] Existing enum numeric values and result-code numeric values are
  unchanged.
- [x] Public symbol count remains `172`.
- [x] Public additions in `v0.10.0`: `0`.
- [x] Public removals in `v0.10.0`: `0`.
- [x] The committed 30 Visualization v2 PNG files are stable-adopted
  byte-for-byte.
- [x] `docs/assets/visualization/manifest.json` is stable-adopted
  byte-for-byte.
- [x] The manifest label remains `0.10.0-alpha visualization v2 evidence` as
  asset provenance.
- [x] Optional Visualization v2 CI artifacts remain manual-only and off for
  push and pull-request CI.
- [x] Python `cffi` remains prototype-only; no wheel is published.
- [x] Rust remains an alpha wrapper; no crate is published.
- [x] Package-manager recipes remain deferred.
- [x] No Pro/cloud sample, network dependency, or external license is used as a
  release gate.
- [x] `original/ruckig-main` remains frozen as Ruckig Community `0.17.3`.

## Public C ABI Review

- [x] `include/ruckig_c/ruckig.h` contains the approved 172-symbol public C ABI
  and no closeout-only public API expansion.
- [x] `docs/abi/public-symbols.txt` contains the 172-symbol public allowlist.
- [x] `docs/abi/public-symbol-exceptions.txt` is unchanged from the `v0.9.0`
  stable baseline.
- [x] ABI artifact paths use `artifacts/abi/0.10.0`.
- [ ] Public header diff against `v0.9.0` is reviewed.
- [ ] Public symbol allowlist verification passes.
- [ ] Public exported-symbol comparison passes.
- [ ] Public additions: `0`.
- [ ] Public removals: `0`.
- [ ] Unapproved exported symbols: `0`.

Public header diff against `v0.9.0`:

```text
Pending final evidence commit.
```

## Visualization Release Evidence

- [x] `v0.10.0` stabilizes `0.10.0-alpha` Visualization v2 gallery evidence.
- [x] `v0.10.0` stabilizes `0.10.0-alpha.2` manual Visualization v2 CI
  artifact evidence.
- [x] `v0.10.0` stabilizes `0.10.0-readiness` local verifier and strict
  regeneration evidence.
- [x] The committed gallery contains 30 PNG files.
- [x] PNG dimensions are `1400x900`.
- [x] The stable release keeps the existing PNG and manifest hashes unchanged.
- [x] V1 gallery provenance remains available through the `v0.9.0` tag.
- [x] Visualization remains local-only and PNG-only.
- [x] The optional CI artifact path remains manual-only.

## Required Local Gates

- [ ] Static Windows clang/Ninja build passes.
- [ ] Shared Windows clang/Ninja build passes.
- [ ] Oracle Windows clang/Ninja build passes.
- [ ] Performance Windows clang/Ninja build passes.
- [ ] Visualization generator and verifier `py_compile` passes.
- [ ] Visualization default verifier passes.
- [ ] Visualization strict regeneration passes.
- [ ] Static CTest without release random passes.
- [ ] Shared CTest without release random passes.
- [ ] Tracking CTest gates pass.
- [ ] Tracking random audit CTest gate passes.
- [ ] Tracking quality hardening CTest gate passes.
- [ ] Tracking stability CTest gate passes.
- [ ] Solver branch coverage CTest gate passes.
- [ ] Fixed oracle suite passes.
- [ ] Ordinary random oracle seeds `1`, `2`, and `41` pass with `100000`
  samples each.
- [ ] Per-DoF random oracle seed `1` passes with `100000` samples.
- [ ] Local release-random CTest passes.
- [ ] Local release-random executable passes with `1000000 --seed 1`.
- [ ] Windows no-waypoint benchmark `--samples 10000 --seed 1` is below the
  average C/oracle ratio threshold `<= 1.5`, or a workstation-noise rerun is
  recorded.
- [ ] Windows waypoint benchmark with fixed seed is recorded as C-only local
  optimizer trend evidence.
- [ ] Local coverage evidence is regenerated under `out/coverage/0.10.0/`.
- [ ] Python prototype smoke passes against the Windows shared build.
- [ ] Rust alpha wrapper smoke passes.
- [ ] Rust alpha examples build under test mode.
- [ ] `docs/assets/visualization` diff remains empty.
- [ ] `original/ruckig-main` diff remains empty.

Local evidence for release-candidate commit:

```text
Pending final evidence commit.
```

## Required CI And Manual Gates

- [ ] Release-candidate push CI succeeds.
- [ ] Manual release-random workflow succeeds on the release-candidate commit.
- [ ] Manual Visualization v2 artifact workflow succeeds on the
  release-candidate commit.
- [ ] Final evidence push CI succeeds.
- [ ] Tag push CI succeeds.
- [ ] Manual release-random workflow succeeds on tag `v0.10.0`.
- [ ] Manual Visualization v2 artifact workflow succeeds on tag `v0.10.0`.
- [ ] GitHub Release `v0.10.0` is published as non-draft and non-prerelease.
- [ ] Windows `clang-cl` C-only.
- [ ] Windows `clang-cl` shared C-only.
- [ ] Windows clang oracle.
- [ ] Linux GCC C-only.
- [ ] Linux Clang oracle.
- [ ] macOS Clang C-only.
- [ ] Windows MinGW static consumer.
- [ ] Windows MinGW DLL consumer.
- [ ] Linux ASan/UBSan.
- [ ] Linux Valgrind.
- [ ] Linux no-waypoint performance.
- [ ] Linux waypoint performance.
- [ ] Linux exported symbols.
- [ ] Windows exported symbols.
- [ ] macOS exported symbols.
- [ ] Python prototype smoke on Windows, Linux, and macOS.
- [ ] Rust alpha wrapper smoke.

Remote evidence before final evidence commit:

```text
Pending final evidence commit.
```

Release operation evidence:

```text
Pending post-release transition commit.
```

## Boundary Checks

- [ ] `docs/assets/visualization` diff remains empty.
- [ ] `original/ruckig-main` diff remains empty.
- [ ] `git diff --check` passes.
- [ ] Coverage artifacts under `out/coverage/0.10.0/` remain untracked.

## Deferred

- Default push or pull-request visualization gate.
- SVG/PDF output.
- Raw sampled-data artifact publication.
- Stable Python package publication.
- Rust crate publication.
- Package-manager recipes.
- Cloud/remote calculation.
- Formal Pro/cloud equivalence claims.
- Formal global optimality proof.
- Soft interruption checkpoints.
- Upstream baseline upgrade.
