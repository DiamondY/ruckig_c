# Ruckig C 0.5.0 Release Checklist

This checklist records the stable `v0.5.0` tracking release closeout. The
release stabilizes the public C tracking ABI added on the `0.5.0-design` line
and ships local Fast-mode online/offline tracking. `RUCKIG_TRACKING_OPTIMIZED`
remains declared for API shape parity but returns `RUCKIG_ERROR_UNSUPPORTED`
throughout `0.5.x`.

## Scope

- [x] Version is `0.5.0` in `CMakeLists.txt`.
- [x] Version macros in `include/ruckig_c/ruckig.h` are `0.5.0`.
- [x] `CHANGELOG.md` has a dated `0.5.0` release entry.
- [x] `README.md` describes `v0.5.0` as the current stable release.
- [x] `docs/current/roadmap.md` records `v0.5.0` as the stable Fast-only
  tracking release and `0.6.0-design` as the next design line.
- [x] Existing `v0.4.2` public functions are not removed or signature-changed.
- [x] Existing enum numeric values and result-code numeric values are
  unchanged.
- [x] The public C tracking ABI is stabilized in `v0.5.0`.
- [x] Local Fast-mode online/offline tracking is included.
- [x] `Optimized` tracking implementation is deferred to `0.6.0-design` and is
  not aliased to Fast.
- [x] Python `cffi` remains prototype-only; no wheel is published.
- [x] Rust remains an alpha wrapper; no crate is published.
- [x] Package-manager recipes remain deferred.
- [x] No Pro/cloud sample, network dependency, or external license is used as a
  release gate.
- [x] `original/ruckig-main` remains frozen as Ruckig Community `0.17.3`.

## Public C ABI Review

- [x] `include/ruckig_c/ruckig.h` contains the approved tracking public C ABI
  and no additional closeout-only public API expansion.
- [x] `docs/abi/public-symbols.txt` contains the 164-symbol public allowlist.
- [x] `docs/abi/public-symbol-exceptions.txt` records the approved tracking
  public additions from the `0.5.0-design` line.
- [x] ABI artifact paths use `artifacts/abi/0.5.0`.
- [x] Public header diff against `v0.4.2` is reviewed:

  ```powershell
  git -c safe.directory=E:/Yww/DownLoad/source/ruckig_c diff v0.4.2 -- include/ruckig_c/ruckig.h
  ```

- [x] Public header diff contains only the approved tracking enum, opaque
  handles, version macros, comments, and public tracking function
  declarations.
- [x] Public symbol allowlist verification passes:

  ```powershell
  cmake --build out\build\windows-clang-ninja-shared --target ruckig_c_verify_public_symbols
  ```

- [x] Public exported-symbol comparison passes:

  ```powershell
  cmake --build out\build\windows-clang-ninja-shared --target ruckig_c_compare_public_exported_symbols
  ```

- [x] Current public symbols: `164`.
- [x] Public removals: `0`.
- [x] Unapproved exported symbols: `0`.
- [x] Tracking internals are not exported.

## Tracking Release Evidence

- [x] C tracking tests cover API lifecycle, validation, online Fast update,
  deterministic fixed corpus, offline sequence calculation, quality smoke, and
  no-allocation paths.
- [x] C tracking examples cover online Fast ramp, online
  constant-acceleration tracking, and offline sequence tracking.
- [x] `Optimized` online and offline calls return `RUCKIG_ERROR_UNSUPPORTED`.
- [x] Tuned ramp and constant-acceleration quality gates are not worse than
  naive instantaneous target chasing under the recorded lag metric.
- [x] Sinus tracking remains trend-only and is not a hard release gate.
- [x] `interrupt_calculation_duration` does not create tracking timeout,
  soft-interruption, or hard real-time guarantees.
- [x] Python prototype smoke covers tracking target states, target sequences,
  online Fast tracking, offline sequence tracking, invalid tracking
  parameters, `Optimized` unsupported behavior, and lifecycle checks.
- [x] Rust alpha smoke covers tracking online/offline behavior, invalid
  tracking parameters, `Optimized` unsupported behavior, and examples.

## Required Local Gates

- [x] Static Windows clang/Ninja build passes:

  ```powershell
  cmake --build --preset windows-clang-ninja
  ```

- [x] Shared Windows clang/Ninja build passes:

  ```powershell
  cmake --build --preset windows-clang-ninja-shared
  ```

- [x] Static CTest without release random passes:

  ```powershell
  ctest --test-dir out\build\windows-clang-ninja --output-on-failure -E ruckig_c_oracle_random_release
  ```

- [x] Shared CTest without release random passes:

  ```powershell
  ctest --test-dir out\build\windows-clang-ninja-shared --output-on-failure -E ruckig_c_oracle_random_release
  ```

- [x] Tracking CTest gates pass:

  ```powershell
  ctest --test-dir out\build\windows-clang-ninja -R ruckig_c_tracking --output-on-failure
  ```

- [x] Fixed oracle suite passes:

  ```powershell
  out\build\windows-clang-ninja-oracle\ruckig_c_oracle_tests.exe
  ```

- [x] Ordinary random oracle seeds `1`, `2`, and `41` pass with `100000`
  samples each.
- [x] Per-DoF random oracle seed `1` passes with `100000` samples.
- [x] Windows no-waypoint benchmark `--samples 10000 --seed 1` is below the
  average C/oracle ratio threshold `<= 1.5`.
- [x] Windows waypoint benchmark with fixed seed is recorded as C-only local
  optimizer trend evidence.
- [x] Python prototype smoke passes against the Windows shared build.
- [x] Rust alpha wrapper smoke passes.

## Required CI And Manual Gates

- [x] Release-candidate push CI succeeds.
- [x] Manual release-random workflow succeeds on the release-candidate commit.
- [ ] Final evidence push CI succeeds.
- [ ] Tag push CI succeeds.
- [ ] Manual release-random workflow succeeds on tag `v0.5.0`.
- [x] Windows `clang-cl` C-only.
- [x] Windows `clang-cl` shared C-only.
- [x] Windows clang oracle.
- [x] Linux GCC C-only.
- [x] Linux Clang oracle.
- [x] macOS Clang C-only.
- [x] Windows MinGW static consumer.
- [x] Windows MinGW DLL consumer.
- [x] Linux ASan/UBSan.
- [x] Linux Valgrind.
- [x] Linux no-waypoint performance.
- [x] Linux waypoint performance.
- [x] Linux exported symbols.
- [x] Windows exported symbols.
- [x] macOS exported symbols.
- [x] Python prototype smoke on Windows, Linux, and macOS.
- [x] Rust alpha wrapper smoke.

## Release Closeout

- [x] Release-candidate implementation commit is created as
  `Release 0.5.0 candidate`.
- [ ] Final evidence commit is created as `Record 0.5.0 release evidence`.
- [ ] Annotated tag `v0.5.0` points to the final evidence commit.
- [ ] GitHub Release `ruckig_c 0.5.0` is published from
  `docs/release/notes/0.5.0.md`.
- [ ] Post-release `main` moves to `0.6.0-design - Unreleased`.
- [ ] `0.5.1` is reserved for emergency patch work only.

## Evidence To Record

```text
Release-candidate commit:
732382a39a57e6ea096d1e14a07144629744e773
(`Release 0.5.0 candidate`).

Final release evidence commit and tag target:
pending final evidence commit.

Public header diff against v0.4.2:
Reviewed. Additions are limited to version macros, the approved tracking enum,
opaque tracking handles, public tracking function declarations, and release
scope comments. No existing `v0.4.2` public symbol is removed or
signature-changed.

Public symbol allowlist:
164 public symbols. Header-derived public symbols match
docs/abi/public-symbols.txt with 0 missing and 0 extra. This is the `v0.4.2`
117-symbol set plus 47 approved tracking symbols.

Windows exported-symbol evidence:
Public exported-symbol comparison is clean. Current exports: 164; approved
public symbols: 164; missing public symbols: 0; removed public symbols: 0;
unapproved exported symbols: 0; exception-approved additions since the v0.2.5
historical baseline: 98.

Linux exported-symbol evidence:
Push CI run `27084086361` succeeded; artifact `7461160970`
(`Linux exported symbols`). The Linux exported-symbol job concluded `success`.

macOS exported-symbol evidence:
Push CI run `27084086361` succeeded; artifact `7461158963`
(`macOS exported symbols`). The macOS exported-symbol job concluded `success`.

Tracking CTest result:
7/7 tracking CTest gates passed locally.

Tracking quality output:
ramp_tuned improvement: 0.173417.
constant_acceleration improvement: 0.972367.
sinus_trend improvement: -0.282661 (trend-only, not a hard gate).

Fixed oracle result:
Oracle comparisons passed: 76.

Waypoint section oracle result:
Waypoint section oracle comparisons passed: 4.

Random oracle result:
100000 seed 1: pass.
100000 seed 2: pass.
100000 seed 41: pass.

Random per-DoF result:
100000 seed 1: pass.

Local release random CTest:
`ruckig_c_oracle_random_release` passed locally in 57.05 seconds.

Release random result before tag:
Manual workflow-dispatch run `27084303942` succeeded against commit
`924f0de7a0ec18b75f1d360e25cda96f4dc348bc` on `main`. Job
`79935641824` (`Manual release random oracle`) concluded `success`.

Final tag release random result:
pending manual release-random workflow on tag v0.5.0.

Windows no-waypoint performance:
average_ratio_c_over_oracle 1.27789; threshold 1.5.

Windows waypoint performance:
waypoint_case_count 10; max DoF 8; max intermediate positions 3; average
3.26093e+06 ns; p99 1.20148e+07 ns; worst 1.77536e+07 ns; C-only local
optimizer corpus.

Python prototype smoke:
20 tests, OK, using the Windows shared build with `RUCKIG_C_SHARED_LIBRARY`
pointing to `out/build/windows-clang-ninja-shared/ruckig_c.dll`.

Rust alpha wrapper smoke:
12 tests, OK. Rust examples test target passed.

GitHub authentication status:
`gh auth status -h github.com` still reports the configured token for
`DiamondY` as invalid, but `git push` and `gh workflow run` both succeeded in
this environment. Continue verifying every remaining remote write operation.

Release-candidate push CI run id:
27084086361.

Release-candidate push CI URL:
https://github.com/DiamondY/ruckig_c/actions/runs/27084086361.

Release-candidate push CI conclusion:
success.

Release-candidate push CI commit:
924f0de7a0ec18b75f1d360e25cda96f4dc348bc.

Release-candidate push CI artifacts:
linux-performance `7461165998`; Linux exported symbols `7461160970`; Windows
exported symbols `7461160228`; macOS exported symbols `7461158963`.

Release-candidate manual release-random run id:
27084303942.

Release-candidate manual release-random URL:
https://github.com/DiamondY/ruckig_c/actions/runs/27084303942.

Release-candidate manual release-random conclusion:
success.

Release-candidate manual release-random job:
`Manual release random oracle`, job id `79935641824`, success.

Final evidence push CI run id:
pending remote CI.

Tag push CI run id:
pending remote CI.

GitHub Release:
pending release publication.
```

## Remote Publication Contingency

If `gh auth` or GitHub network access is unavailable, stop after the
release-candidate commit and local evidence. Do not create a tag or GitHub
Release without reliable CI and manual workflow evidence. Record the blocked
remote steps for manual execution.

## Deferred

- Bounded local `Optimized` tracking implementation for `0.6.0-design`.
- Soft interruption checkpoints and timeout fallback semantics.
- Formal Python package and wheel publication.
- Rust crate publication.
- Package-manager recipes.
- Cloud or remote calculation.
- Formal Pro/cloud numerical equivalence claims.
- Upstream baseline upgrade.
