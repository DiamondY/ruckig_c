# 0.16.0-alpha.1 Public Diagnostics Design Checklist

Status: docs-only design complete; ordinary remote push CI is observed after
the checklist commit is pushed.

This checklist starts the `0.16.0-design-public-diagnostics` line as a design
artifact only. It does not implement public diagnostics, change version
metadata, expand the 184-symbol `v0.15.0` public C ABI baseline, create a tag,
publish wrappers, change workflow behavior, edit ABI allowlists, update the
upstream baseline, or touch visualization assets.

## Scope

- [x] Added `docs/design/0.16.0_public_diagnostics.md`.
- [x] Recorded public diagnostics goals for invalid input, interruption,
  resume mismatch, tracking candidate diagnostics, and waypoint resume
  diagnostics.
- [x] Listed candidate public types, diagnostic codes, and opt-in API entry
  points for future implementation review.
- [x] Kept existing result-code numeric values frozen.
- [x] Kept existing public struct layouts frozen.
- [x] Kept Python and Rust wrappers prototype-only.
- [x] Deferred implementation to a future reviewed `0.16.0` implementation
  slice.

## Public API / ABI Boundary

- [x] No exported C function is added in this slice.
- [x] No public function signature is changed in this slice.
- [x] No enum numeric value or result-code numeric value is changed in this
  slice.
- [x] No public struct layout or public diagnostics layout is changed in this
  slice.
- [x] `include/ruckig_c/ruckig.h` is unchanged.
- [x] No ABI allowlist or public-symbol exception file is changed.
- [x] No version metadata, tag, GitHub Release, package-manager recipe,
  workflow, upstream baseline, or visualization asset is changed.

## Future Implementation Requirements

- [ ] Any implementation slice must explicitly update the public header and ABI
  allowlist in the same reviewed commit.
- [ ] Any implementation slice must include C and C++ header compile tests for
  new diagnostics symbols.
- [ ] Any implementation slice must add fixed public behavior tests for
  invalid-input, zero-limit, current/target limit, waypoint resume mismatch,
  tracking sequence resume mismatch, and interruption diagnostics.
- [ ] Any implementation slice must prove exported symbols only add the
  accepted diagnostics symbols.
- [ ] Wrapper stabilization remains outside this design slice.

## Local Gates

| Gate | Result |
| --- | --- |
| `cmake --build --preset windows-clang-ninja` | Pass |
| `ctest --test-dir out\build\windows-clang-ninja --output-on-failure -R "ruckig_c_header_c\|ruckig_c_header_cpp"` | Pass, 2/2 |

## Boundary Checks

| Check | Result |
| --- | --- |
| `git diff -- include/ruckig_c/ruckig.h docs/abi/public-symbols.txt docs/abi/public-symbol-exceptions.txt docs/abi/exceptions.md .github/workflows/ci.yml` | Clean |
| `git diff -- original/ruckig-main docs/assets/visualization` | Clean |
| `git diff --check` | Pass; Git reported expected CRLF normalization warnings only |

## Remote CI

Ordinary remote push CI is observed after pushing this checklist commit to
`main`. The run URL and conclusion should be recorded in the delivery summary.
