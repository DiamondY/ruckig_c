# Ruckig C 0.2.5 Release Checklist

This checklist records the `v0.2.5` patch-release closeout evidence. Local
release gates were rerun from the `0.2.5` release candidate state. Remote push
CI, manual release-random, tag, and GitHub Release evidence are recorded below.

## Scope

- [x] Final commit hash recorded.
- [x] Version is `0.2.5` in `CMakeLists.txt` if a `v0.2.5` tag is being cut.
- [x] Version macros in `include/ruckig_c/ruckig.h` are `0.2.5` if a
  `v0.2.5` tag is being cut.
- [x] `CHANGELOG.md` has a dated `0.2.5` release entry.
- [x] No public C API additions unless separately approved.
- [x] Public header diff reviewed against `v0.2.4`.
- [x] Linux exported-symbol baseline diff reviewed against
  `docs/abi/v0.2.4/linux-symbols.txt`.
- [x] Windows exported-symbol baseline diff reviewed against
  `docs/abi/v0.2.4/windows-symbols.txt`.
- [x] Result-code and enum numeric values unchanged.
- [x] No unintended public API additions.
- [x] Strict ABI diff gate remains warning/evidence-only unless all documented
  prerequisites and exception-policy requirements are satisfied.
- [x] Intermediate waypoints remain unsupported.
- [x] Per-section constraints remain unsupported.
- [x] Cloud calculation remains unsupported.
- [x] Python binding implementation and Rust bindings remain deferred.
- [x] Package-manager recipes remain deferred.
- [x] `original/ruckig-main` remains frozen as the Ruckig Community `0.17.3`
  oracle baseline.

## Required Gates

- [x] `git status --short --branch` is clean before release closeout edits.
- [x] `git status --short --branch` is clean after the release closeout commit
  and before tagging.
- [x] Static CMake release tests pass on Windows clang/Ninja.
- [x] Shared-library release tests pass on Windows clang/Ninja.
- [x] Fixed C++ oracle suite passes and reports the final case count.
- [x] Routine per-DoF random smoke passes with `--random-per-dof 100 --seed 1`.
- [x] Ordinary development random oracle passes with `--random 100000 --seed 1`.
- [x] Ordinary development random oracle passes with `--random 100000 --seed 2`.
- [x] Ordinary development random oracle passes with `--random 100000 --seed 41`.
- [x] Per-DoF development random oracle passes with
  `--random-per-dof 100000 --seed 1`.
- [x] Release random oracle passes with `--random 1000000 --seed 1`.
- [x] Windows release performance benchmark average ratio is `<= 1.5`.
- [x] Linux release performance benchmark average ratio is `<= 1.5`.
- [x] Windows performance trend is compared against the `0.2.4` Windows
  release baseline.
- [x] Linux performance trend is compared against the `0.2.4` Linux release
  baseline.
- [x] GitHub Actions push CI passes for the release evidence commit.
- [x] GitHub Actions manual release random oracle passes with
  `release_random=true`.
- [x] Linux Clang ASan+UBSan passes.
- [x] Linux Valgrind passes.
- [x] Installed CMake consumer smoke test passes.
- [x] Linux pkg-config consumer smoke test passes.
- [x] Unix shared install-tree consumer CTest
  `ruckig_c_shared_install_consumer` passes where `pkg-config` is available.
- [x] Windows manual static consumer smoke test
  `ruckig_c_windows_manual_static_consumer` passes where enabled.
- [x] Windows DLL consumer smoke test `ruckig_c_windows_dll_consumer` passes
  where enabled.
- [x] Windows `clang-cl` static consumer smoke passes in CI.
- [x] Windows `clang-cl` shared DLL consumer smoke passes in CI.
- [x] MSVC `cl` standalone static smoke is either passed or explicitly marked
  as not yet verified.
- [x] MSVC `cl` standalone DLL smoke is either passed or explicitly marked as
  not yet verified.
- [x] MinGW static/DLL consumer status is explicitly marked as not yet verified
  unless evidence exists.
- [x] Shared build target `ruckig_c_exported_symbols` produces a review
  artifact.
- [x] Shared build target `ruckig_c_compare_exported_symbols` produces a
  non-strict ABI diff artifact.
- [x] GitHub Actions `Linux exported symbols` and `Windows exported symbols`
  jobs upload current export and baseline diff artifacts.
- [x] `v0.2.5` annotated tag points at the final release closeout commit.
- [x] GitHub Release `ruckig_c 0.2.5` is published.

## Evidence To Record

```text
Final release commit:
c45a6ece69921c26419efcaefe10eed87de03605.
Annotated tag object:
7f11fb1e7ad3b513c3911c16a7699fc0bcd0b9bf.
Tag target commit:
c45a6ece69921c26419efcaefe10eed87de03605.
Push CI run id:
26965537200.
Push CI URL:
https://github.com/DiamondY/ruckig_c/actions/runs/26965537200.
Push CI conclusion:
success.
Manual release random run id:
26965856552.
Manual release random URL:
https://github.com/DiamondY/ruckig_c/actions/runs/26965856552.
Manual release random conclusion:
success.
Windows static CTest result:
100% tests passed, 0 tests failed out of 20.
Windows shared CTest result:
100% tests passed, 0 tests failed out of 20.
Fixed oracle suite result:
Oracle comparisons passed: 74.
Ordinary random seed 1 result:
Oracle comparisons passed: 74; Random oracle comparisons passed: 100000 seed 1.
Ordinary random seed 2 result:
Oracle comparisons passed: 74; Random oracle comparisons passed: 100000 seed 2.
Ordinary random seed 41 result:
Oracle comparisons passed: 74; Random oracle comparisons passed: 100000 seed 41.
Per-DoF random seed 1 result:
Oracle comparisons passed: 74; Random per-DoF oracle comparisons passed: 100000 seed 1.
Release random oracle result:
100% tests passed, 0 tests failed out of 1; `ruckig_c_oracle_random_release` passed in 48.99 seconds.
Windows performance result:
average_ratio_c_over_oracle: 1.16244; threshold: 1.5.
Linux performance result:
Manual workflow run `26965856552` completed successfully; Linux Clang performance average ratio: 1.30314; threshold: 1.5.
Windows/Linux performance trend versus 0.2.4:
Windows local release ratio changed from `1.4087` in `0.2.4` to `1.16244` in `0.2.5`; both are below the `1.5` threshold. Linux manual workflow ratio changed from `1.29012` in `0.2.4` to `1.30314` in `0.2.5`; both are below the `1.5` threshold.
Consumer automation result:
Installed CMake consumer, Windows manual static consumer, and Windows DLL consumer passed in local release-check CTest. Linux pkg-config and shared install-tree consumers passed in GitHub Actions push/manual CI.
MSVC cl standalone consumer result:
Documented, not yet CI-verified.
MinGW consumer result:
Not yet verified.
Linux exported-symbol artifact:
Manual workflow run `26965856552`, artifact id `7417437366` (`Linux exported symbols`).
Windows exported-symbol artifact:
`build_release_check_shared/artifacts/abi/0.2.5/windows-exports.txt`; manual workflow run `26965856552`, artifact id `7417439659` (`Windows exported symbols`).
Linux exported-symbol diff artifact:
Manual workflow run `26965856552`, artifact id `7417437366` (`Linux exported symbols`).
Windows exported-symbol diff artifact:
`build_release_check_shared/artifacts/abi/0.2.5/windows-export-diff.txt`.
Exported symbol review result:
Windows exported-symbol baseline comparison reports 66 current symbols, 66 baseline symbols, 0 added symbols, and 0 removed symbols. Linux raw exported-symbol comparison reports 127 current symbols, 66 baseline symbols, 61 added implementation-internal `ruckig_*` symbols, and 0 removed symbols. This was reviewed as warning/evidence-only; the public header still has no public API additions, and `docs/abi/v0.2.5/linux-symbols.txt` records the actual Linux shared export baseline for future drift review.
Strict ABI gate decision:
Warning/evidence-only unless all documented prerequisites are satisfied.
Public header diff review result:
Public header diff from `v0.2.4` contains only version macro changes from `0.2.4` to `0.2.5`; public functions, enum numeric values, and result-code numeric values are unchanged.
Enum/result-code review result:
Enum and result-code numeric values unchanged.
GitHub Release URL:
https://github.com/DiamondY/ruckig_c/releases/tag/v0.2.5.
```

## ABI Review Commands

```powershell
git -c safe.directory=E:/Yww/DownLoad/source/ruckig_c diff v0.2.4 -- include/ruckig_c/ruckig.h
cmake --build build_release_check_shared --target ruckig_c_exported_symbols
cmake --build build_release_check_shared --target ruckig_c_compare_exported_symbols
```

The comparison target remains warning/evidence only unless the strict gate
requirements in `docs/current/api_compatibility.md` are explicitly satisfied
and the release checklist records that decision.

Tag-time review artifacts:

```text
build_release_check_shared/artifacts/abi/0.2.5/windows-exports.txt
build_release_check_shared/artifacts/abi/0.2.5/windows-export-diff.txt
build-shared/artifacts/abi/0.2.5/linux-exports.txt
build-shared/artifacts/abi/0.2.5/linux-export-diff.txt
```

After publication, `main` moves the next-stage ABI comparison artifacts to
`artifacts/abi/0.3.0-design/` and compares against `docs/abi/v0.2.5/`.
