# 0.17.0 Wrapper Stabilization Decision Checklist

Status: local verification complete; ordinary remote push CI is observed after
the checklist commit is pushed.

This checklist records the docs-only
`0.17.0-wrapper-stabilization-decision` slice. It reviews the wrapper public
diagnostics prototype smoke and consumer install refresh evidence, then keeps
Python and Rust wrappers prototype-only. It does not change runtime behavior,
public API, public ABI, version metadata, release/tag state, workflow behavior,
package recipes, wrapper publication status, upstream baseline, or
visualization assets.

## Scope

- [x] Added `docs/design/0.17.0_wrapper_stabilization_decision.md`.
- [x] Reviewed post-`v0.16.0` Python/Rust public diagnostics prototype smoke
  evidence.
- [x] Reviewed installed CMake/pkg-config consumer diagnostics smoke evidence.
- [x] Recorded that Python remains a `cffi` ABI-mode prototype.
- [x] Recorded that Rust remains an unpublished alpha wrapper.
- [x] Deferred Python-first, Rust-first, dual-wrapper stabilization, and
  continued prototype-only choice to a later explicit implementation plan.

## Decision

- [x] Continue prototype-only wrapper status.
- [x] Do not publish Python wheels.
- [x] Do not publish a Rust crate.
- [x] Do not claim stable wrapper APIs.
- [x] Do not add package-manager recipes.
- [x] Do not change the public C ABI or public header.

## Public API / ABI Boundary

- [x] No exported C function is added.
- [x] No public C function signature is changed.
- [x] No enum numeric value or result-code numeric value is changed.
- [x] No public struct layout or public diagnostics layout is changed.
- [x] `include/ruckig_c/ruckig.h` is unchanged.
- [x] No ABI allowlist or public-symbol exception file is changed.
- [x] No version metadata, tag, GitHub Release, package-manager recipe,
  workflow, upstream baseline, or visualization asset is changed.

## Local Gates

| Gate | Result |
| --- | --- |
| `cmake --build --preset windows-clang-ninja` | Pass; no rebuild work |
| `ctest --test-dir out\build\windows-clang-ninja --output-on-failure -R "ruckig_c_header_c\|ruckig_c_header_cpp\|ruckig_c_public_diagnostics"` | Pass, 3/3 |

## Boundary Checks

| Check | Result |
| --- | --- |
| `git diff -- include/ruckig_c/ruckig.h docs/abi/public-symbols.txt docs/abi/public-symbol-exceptions.txt docs/abi/exceptions.md .github/workflows/ci.yml` | Clean |
| `git diff -- original/ruckig-main docs/assets/visualization` | Clean |
| `git diff --check` | Pass; Git reported expected CRLF normalization warnings only |

## Remote CI

Ordinary remote push CI is observed after pushing this checklist commit to
`main`. The run URL and conclusion should be recorded in the delivery summary.
