# 0.15.0-alpha.7 Tracking Sequence Continuation Wrapper Coverage

Status: local alpha wrapper smoke complete.

This slice adds user-facing smoke coverage for the `0.15.0-alpha.4` through
`0.15.0-alpha.6` interruptible tracking sequence continuation API. It keeps
the C ABI at the 184-symbol alpha.4 baseline and does not publish Python/Rust
packages.

## Scope

- [x] Adds a C example for interruptible Optimized tracking sequence
  continuation.
- [x] Wires the example into CMake, the `ruckig_c_examples` target, and
  focused CTest coverage.
- [x] Adds Python cffi prototype declarations, a continuation wrapper class,
  interruptible start/resume methods, and smoke coverage.
- [x] Adds Rust FFI declarations, a safe continuation wrapper, interruptible
  start/resume methods, unit smoke coverage, and an example.
- [x] Keeps wrappers prototype-only with no wheel, crate publication,
  package-manager recipe, or stable wrapper API claim.

## Public API / ABI Boundary

- [x] No public C symbols are added beyond the 184-symbol alpha.4 baseline.
- [x] No enum numeric, result-code numeric, public diagnostics struct layout,
  or public header signature change is added by alpha.7.
- [x] No version metadata, tag, GitHub Release, push, or manual workflow.
- [x] `ruckig_tracking_calculate_sequence` keeps complete-solve semantics; the
  continuation behavior is exposed only through the alpha.4 interruptible and
  resume entry points.

## Local Gates

| Gate | Result |
| --- | --- |
| `cmake --build --preset windows-clang-ninja` | Pass |
| `ctest --test-dir out\build\windows-clang-ninja --output-on-failure -R "example_ruckig_c_23_tracking_sequence_continuation\|ruckig_c_examples_tracking_sequence_continuation\|ruckig_c_tracking_sequence_fast_continuation\|ruckig_c_tracking_sequence_optimized_continuation\|ruckig_c_tracking_sequence_continuation_api"` | Pass, 5/5 tests passed |
| `cmake --build --preset windows-clang-ninja-shared` | Pass |
| `python bindings\python_prototype\test_prototype.py` with `RUCKIG_C_SHARED_LIBRARY=out\build\windows-clang-ninja-shared\ruckig_c.dll` | Pass, 24 tests passed |
| `cargo test --manifest-path bindings\rust\Cargo.toml` | Pass, 16 unit tests passed |
| `cargo test --manifest-path bindings\rust\Cargo.toml --examples` | Pass, 10 examples compiled and ran |

## Final Gates

| Gate | Result |
| --- | --- |
| `ctest --test-dir out\build\windows-clang-ninja --output-on-failure` | Pass, 61/61 tests passed |
| `cmake --build out\build\windows-clang-ninja-shared --target ruckig_c_verify_public_symbols` | Pass, header and allowlist both 184 symbols; missing `0`, extra `0` |
| `cmake --build out\build\windows-clang-ninja-shared --target ruckig_c_compare_public_exported_symbols` | Pass, exported symbols match the approved allowlist; unapproved exports `0` |
| Boundary diffs and `git diff --check` | Pass; public header, ABI docs, CI workflow, upstream baseline, and visualization assets unchanged in alpha.7 |

## Deferred

- Remote cumulative CI evidence for alpha.4 through alpha.7.
- Stable `0.15.0` readiness and release closeout.
- Package-manager recipes, wheel/crate publication, formal wrapper API
  commitment, Cloud/Pro claims, hard real-time guarantees, formal global
  optimality proof, upstream baseline upgrade, runtime clock public hooks,
  tags, releases, pushes, and manual workflows.
