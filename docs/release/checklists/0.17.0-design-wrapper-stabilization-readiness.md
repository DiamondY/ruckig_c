# 0.17.0 Design Wrapper Stabilization Readiness Checklist

Status: local verification complete; ordinary remote push CI is observed after
the checklist commit is pushed.

This checklist records the `0.17.0-design-wrapper-stabilization-readiness`
slice. It is docs-only and evaluates whether Python or Rust wrappers are ready
to move from prototype smoke evidence toward stable publication. It does not
change library runtime behavior, public API, public ABI, version metadata,
release/tag state, workflow behavior, package-manager scope, wrapper
publication status, upstream baseline, or visualization assets.

## Scope

- [x] Added `docs/design/0.17.0_wrapper_stabilization_readiness.md`.
- [x] Chose a criteria-first readiness conclusion.
- [x] Kept Python and Rust wrappers prototype-only.
- [x] Did not recommend Python-first, Rust-first, or dual-wrapper
  implementation by default.
- [x] Recorded shared-library/static-library discovery as a blocker for stable
  wrapper publication.
- [x] Recorded package policy, C ABI compatibility, diagnostics mapping,
  lifecycle, array, CI, and release-evidence criteria.
- [x] Recorded Python-specific gaps for cffi/extension choice, wheel policy,
  package namespace, exceptions, diagnostics mapping, and optional NumPy.
- [x] Recorded Rust-specific gaps for crate ownership, linking policy, safe FFI
  wrappers, feature flags, and `unsafe` boundaries.

## Public API / ABI Boundary

- [x] No exported C function is added.
- [x] No public function signature is changed.
- [x] No enum numeric value or result-code numeric value is changed.
- [x] No public struct layout or public diagnostics layout is changed.
- [x] `include/ruckig_c/ruckig.h` is unchanged.
- [x] No ABI allowlist or public-symbol exception file is changed.
- [x] No version metadata, tag, GitHub Release, package-manager recipe,
  workflow, upstream baseline, or visualization asset is changed.
- [x] Python and Rust wrappers remain prototype-only.

## Local Gates

| Gate | Result |
| --- | --- |
| `cmake --build --preset windows-clang-ninja` | Pass |
| `ctest --test-dir out\build\windows-clang-ninja --output-on-failure -R "ruckig_c_header_c\|ruckig_c_header_cpp\|ruckig_c_public_diagnostics"` | Pass, 3/3 |

## Boundary Checks

| Check | Result |
| --- | --- |
| `git diff -- include/ruckig_c/ruckig.h docs/abi/public-symbols.txt docs/abi/public-symbol-exceptions.txt docs/abi/exceptions.md .github/workflows/ci.yml` | Clean |
| `git diff -- original/ruckig-main docs/assets/visualization` | Clean |
| `git diff --check` | Pass; Git reported expected CRLF normalization warnings only |

## Remote CI

Ordinary remote push CI is observed after pushing this checklist commit to
`main`. The run URL and conclusion should be recorded in the delivery summary.
