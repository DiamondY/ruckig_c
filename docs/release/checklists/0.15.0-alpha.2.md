# 0.15.0-alpha.2 Tracking Sequence Interruption API Draft

Status: docs-only local evidence.

This slice adds a quasi-spec for possible future
`ruckig_tracking_calculate_sequence` interruption. It does not implement
runtime behavior, add tests, change public headers, expand ABI, push, tag,
publish a release, bump version metadata, or trigger manual workflows.

## Scope

- [x] Added `docs/design/tracking_sequence_interruption_api.md`.
- [x] Documents why the current API-neutral path cannot cleanly carry sequence
  interruption.
- [x] Records candidate public API shapes: sequence-level interruption flag,
  partial sequence status, diagnostics snapshot extension, and explicit
  continuation carrier.
- [x] Records ABI, migration, diagnostics, testing, and release risks.
- [x] Recommends no implementation in `0.15.0-alpha.2`; any accepted public API
  expansion must be a separate `0.15.0-alpha.4` or later plan.

## Boundary

- [x] No change to `include/ruckig_c/ruckig.h`.
- [x] No new public symbols, exported symbols, public structs, enum values,
  result-code values, or ABI allowlist changes.
- [x] No C source, CMake test, workflow, package-manager, upstream baseline, or
  visualization asset change.
- [x] No push, tag, GitHub Release, version bump, or manual workflow.

## Local Checks

| Check | Result |
| --- | --- |
| `git diff --check` | Passed |
| Public header / ABI / workflow / CMake boundary diff | Passed, no diff |
| `original/ruckig-main` boundary diff | Passed, no diff |
| `docs/assets/visualization` boundary diff | Passed, no diff |

## Deferred

- Tracking sequence interruption remains deferred until a separate public
  diagnostics/API decision is accepted.
- Runtime clock public hooks, public resume diagnostics, package-manager
  recipes, formal Python/Rust publication, Cloud/Pro claims, hard real-time
  guarantees, formal global optimality proof, and upstream baseline upgrades
  remain deferred.
