# ruckig_c 0.7.0

`0.7.0` is the stable Optimized tracking strategy and diagnostics release. It
stabilizes the 172-symbol public C ABI reviewed during `0.7.0-readiness`: the
bounded local Optimized tracking API from `v0.6.0`, high-level strategy preset
controls, and the public tracking diagnostics snapshot.

## Highlights

- Stabilized `Stable`, `Balanced`, and `Aggressive` Optimized tracking
  strategy presets.
- Stabilized `ruckig_tracking_set_optimized_strategy` and
  `ruckig_tracking_get_optimized_strategy`.
- Stabilized `ruckig_tracking_diagnostics_t` and
  `ruckig_tracking_get_last_diagnostics`.
- Kept bounded deterministic local Optimized tracking with Fast fallback and
  observable diagnostics.
- Retained no-waypoint solver, waypoint optimizer, tracking quality,
  ABI/export, coverage, and wrapper smoke gates from the `0.7.0-readiness`
  evidence line.
- Kept Python `cffi` and Rust wrapper work as prototype/alpha evidence only.

## Compatibility

- Public C ABI additions since `v0.6.0`: 3 approved strategy/diagnostics
  symbols.
- Public C symbol allowlist: 172 symbols.
- Existing `v0.6.0` public functions, signatures, enum numeric values, and
  result-code numeric values are unchanged.
- `RUCKIG_TRACKING_OPTIMIZED_STABLE = 0`.
- `RUCKIG_TRACKING_OPTIMIZED_BALANCED = 1`.
- `RUCKIG_TRACKING_OPTIMIZED_AGGRESSIVE = 2`.
- `RUCKIG_TRACKING_OPTIMIZED` is a bounded deterministic local evaluator, not
  a formal global optimizer.
- Fast fallback is observable through tracking diagnostics.
- `interrupt_calculation_duration` does not create tracking timeout,
  soft-interruption, or hard real-time guarantees.
- `original/ruckig-main` remains frozen as Ruckig Community `0.17.3`.

## Evidence Summary

- Fixed no-waypoint oracle comparison count: 82.
- Waypoint section-oracle comparison count: 4.
- Ordinary random oracle: `100000` cases each for seeds `1`, `2`, and `41`.
- Per-DoF random oracle: `100000` cases for seed `1`.
- Local release-random CTest: `ruckig_c_oracle_random_release` passed in
  58.27 seconds.
- Local release-random executable: `1000000` cases for seed `1`.
- Manual release-random oracle before tag:
  [27123988351](https://github.com/DiamondY/ruckig_c/actions/runs/27123988351),
  success on `main` commit `04bd5c285644b765604cb7f617087266886486fc`; job
  `80047736905` (`Manual release random oracle`) succeeded.
- Tracking CTest gates: 11/11 passed.
- Solver branch coverage CTest gate: 1/1 passed.
- Windows no-waypoint performance: average C/oracle ratio `1.38442`, below
  threshold `1.5`.
- Windows waypoint performance: C-only local optimizer corpus, 10 case
  families, max 8 DoF, max 3 intermediate waypoints, average `3.31579e+06` ns,
  p99 `1.23908e+07` ns, worst `1.79486e+07` ns.
- Coverage summary: line `87.71%`, function `91.86%`, branch `69.26%`, region
  `87.44%` under `out/coverage/0.7.0/`.
- Public symbol allowlist verification: clean; header public symbols 172,
  expected public symbols 172, missing 0, extra 0.
- Public exported-symbol comparison: clean; current exports 172, approved
  public symbols 172, missing public 0, removed public 0, unapproved exported
  0.
- Python prototype smoke: 21 tests, OK.
- Rust alpha wrapper smoke: 13 tests, OK, plus examples target passed.
- Release-candidate implementation commit:
  `04bd5c285644b765604cb7f617087266886486fc`.
- Final evidence commit and tag target:
  `0f7a3bd46c70367c3bf2bfb4814167612d7a7634`.
- Annotated tag object:
  `2da688da14b851242c4fcd475872615978d848fc`.
- Release-candidate push CI:
  [27123644850](https://github.com/DiamondY/ruckig_c/actions/runs/27123644850),
  success on commit `04bd5c285644b765604cb7f617087266886486fc`.
- Final evidence push CI:
  [27124597295](https://github.com/DiamondY/ruckig_c/actions/runs/27124597295),
  success on commit `0f7a3bd46c70367c3bf2bfb4814167612d7a7634`.
- Tag push CI:
  [27125048641](https://github.com/DiamondY/ruckig_c/actions/runs/27125048641),
  success on tag `v0.7.0`, target commit
  `0f7a3bd46c70367c3bf2bfb4814167612d7a7634`.
- Manual release-random oracle on tag:
  [27125459842](https://github.com/DiamondY/ruckig_c/actions/runs/27125459842),
  success on `v0.7.0` target commit
  `0f7a3bd46c70367c3bf2bfb4814167612d7a7634`; job `80052773403`
  (`Manual release random oracle`) succeeded.
- Exported-symbol CI artifacts from release-candidate push: Linux
  `7474363778`, Windows `7474362720`, macOS `7474357074`.
- Linux performance CI artifact from release-candidate push: `7474375572`.
- Exported-symbol CI artifacts from final evidence push: Linux `7474737621`,
  Windows `7474742662`, macOS `7474734403`.
- Exported-symbol CI artifacts from tag push: Linux `7474916214`, Windows
  `7474922921`, macOS `7474910859`.
- Linux performance CI artifact from final evidence push: `7474752578`.
- Linux performance CI artifact from tag push: `7474924499`.

- GitHub Release:
  [ruckig_c 0.7.0](https://github.com/DiamondY/ruckig_c/releases/tag/v0.7.0),
  published as a stable release, not a draft and not a prerelease.
- Post-release `main` moves to `0.8.0-design - Unreleased`, focused first on
  local algorithm visualization and trajectory gallery evidence. `0.7.1` is
  reserved for emergency patch work only.

## Deferred

- Algorithm visualization and trajectory gallery generation.
- Formal proof of global optimality is not provided.
- Formal Ruckig Pro/cloud numerical equivalence is not claimed.
- Cloud and remote calculation are not implemented.
- No hard or soft real-time tracking guarantee is made.
- Soft interruption checkpoints are deferred.
- Python wheels are not published.
- Rust crate publication is deferred.
- Package-manager recipes are deferred.
- Upstream baseline upgrade is deferred.
