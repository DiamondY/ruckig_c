# ruckig_c 0.5.0

`0.5.0` is the stable tracking Fast-mode release. It stabilizes the public C
tracking ABI introduced on the `0.5.0-design` line and ships local online and
offline Fast tracking. `RUCKIG_TRACKING_OPTIMIZED` remains declared for API
shape parity but returns `RUCKIG_ERROR_UNSUPPORTED` throughout `0.5.x`; the
bounded local `Optimized` implementation is deferred to `0.6.0-design`.

## Highlights

- Stabilized the public C tracking ABI:
  - `ruckig_tracking_t`
  - `ruckig_target_state_t`
  - `ruckig_target_state_sequence_t`
  - `ruckig_tracking_output_sequence_t`
  - online `ruckig_tracking_update`
  - offline `ruckig_tracking_calculate_sequence`
- Included local Fast-mode tracking using deterministic constant-acceleration
  lookahead.
- Included offline tracking output sequences for position, velocity,
  acceleration, jerk, time, section, and per-step result code.
- Kept `Optimized` tracking unsupported in `0.5.x`; it is not aliased to Fast.
- Promoted ABI artifact paths to `artifacts/abi/0.5.0`.
- Retained no-waypoint solver and waypoint optimizer regression gates from the
  prior stable releases.
- Kept Python `cffi` and Rust wrapper work as prototype/alpha evidence only.

## Compatibility

- Public C ABI additions since `v0.4.2`: 47 approved tracking symbols.
- Public C symbol allowlist: 164 symbols.
- Existing `v0.4.2` public functions, signatures, enum numeric values, and
  result-code numeric values are unchanged.
- `RUCKIG_TRACKING_FAST = 0`.
- `RUCKIG_TRACKING_OPTIMIZED = 1`.
- `Optimized` online/offline calls return `RUCKIG_ERROR_UNSUPPORTED`.
- `interrupt_calculation_duration` does not create tracking timeout,
  soft-interruption, or hard real-time guarantees.
- `original/ruckig-main` remains frozen as Ruckig Community `0.17.3`.

## Evidence Summary

- Fixed no-waypoint oracle comparison count: 76.
- Waypoint section-oracle comparison count: 4.
- Ordinary random oracle: `100000` cases each for seeds `1`, `2`, and `41`.
- Per-DoF random oracle: `100000` cases for seed `1`.
- Local release-random CTest: `ruckig_c_oracle_random_release` passed in
  57.05 seconds.
- Manual release-random oracle before tag:
  [27084303942](https://github.com/DiamondY/ruckig_c/actions/runs/27084303942),
  success on `main` commit `924f0de7a0ec18b75f1d360e25cda96f4dc348bc`; job
  `79935641824` (`Manual release random oracle`) succeeded.
- Tracking CTest gates: 7/7 passed.
- Tracking quality:
  - ramp tuned improvement: `0.173417`.
  - constant-acceleration improvement: `0.972367`.
  - sinus trend improvement: `-0.282661`, trend-only.
- Windows no-waypoint performance: average C/oracle ratio `1.27789`, below
  threshold `1.5`.
- Windows waypoint performance: C-only local optimizer corpus, 10 case
  families, max 8 DoF, max 3 intermediate waypoints, average `3.26093e+06` ns,
  p99 `1.20148e+07` ns, worst `1.77536e+07` ns.
- Linux no-waypoint and waypoint performance: push CI run
  [27084086361](https://github.com/DiamondY/ruckig_c/actions/runs/27084086361)
  succeeded; `linux-performance` artifact id `7461165998`.
- Public symbol allowlist verification: clean; header public symbols 164,
  expected public symbols 164, missing 0, extra 0.
- Public exported-symbol comparison: clean; current exports 164, approved
  public symbols 164, missing public 0, removed public 0, unapproved exported
  0.
- Python prototype smoke: 20 tests, OK.
- Rust alpha wrapper smoke: 12 tests, OK, plus examples target passed.
- Release-candidate local evidence commit:
  `732382a39a57e6ea096d1e14a07144629744e773`.
- Release-candidate push CI:
  [27084086361](https://github.com/DiamondY/ruckig_c/actions/runs/27084086361),
  success on commit `924f0de7a0ec18b75f1d360e25cda96f4dc348bc`.
- Final evidence push CI: pending.
- Tag push CI: pending.
- Tag manual release-random workflow: pending.
- Final tag target: pending.
- GitHub Release: pending.
- Exported-symbol CI artifacts: Linux `7461160970`, Windows `7461160228`,
  macOS `7461158963`.
- GitHub authentication: local `gh auth status -h github.com` still reports
  an invalid token, but `git push` and `gh workflow run` succeeded; remaining
  tag and release operations must be verified independently.

## Remote Publication Contingency

Remote release publication requires each GitHub write operation to succeed and
be verified. The local `gh auth status` command still reports an invalid token,
so tag push, GitHub Release publication, and post-release `0.6.0-design`
transition must be checked explicitly before closing the release.

## Deferred

- Bounded local `Optimized` tracking implementation is deferred to
  `0.6.0-design`.
- Soft interruption checkpoints are deferred.
- Cloud and remote calculation are not implemented.
- No formal Ruckig Pro/cloud numerical equivalence claim is made.
- No hard or soft real-time tracking guarantee is made.
- Python wheels are not published.
- Rust crate publication is deferred.
- Package-manager recipes are deferred.
- Upstream baseline upgrade is deferred.
